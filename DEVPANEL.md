# لوحة التحكم المركزية — `/devpanel`

كل الإدارة من مكان واحد بدل أوامر متفرقة.

```
/devpanel
```

(نفس الأمر: `/لوحة` أو `/panel` — ثلاثتها توصل لنفس اللوحة)

---

## الفكرة

بدل ما تحفظ عشرات الأوامر لكل نظام، تفتح اللوحة مرة وحدة وتتنقّل بالأزرار:

```
/devpanel
   ↓
اختر القسم (زر)
   ↓
إعدادات القسم (أزرار / قوائم / نافذة إدخال)
   ↓
رجوع → القائمة الرئيسية (بلا حاجة لإعادة /devpanel)
```

كل خطوة تُعدّل نفس الرسالة — ما تتكدّس رسائل جديدة في القناة.

---

## الأقسام المتاحة

| القسم | الزر | يشمل |
|---|---|---|
| الإدارة | 🛡️ | الحظر، الطرد، الإسكات، التحذير |
| التذاكر | 🎫 | الكاتيغوري، الأرشيف، التقييم، الإغلاق التلقائي، حد الاستلام |
| السلم الإداري | 👥 | عرض الرتب ومستوياتها |
| الإجازات والبلاغات | 🌴 | تفعيل/تعطيل كل من الإجازات والاستقالات والبلاغات، وقنواتها |
| التقارير | 📊 | تفعيل التقرير الدوري وقناته |
| بناء الإمبيدات | 🎨 | ملخص الإمبيدات والأوامر المخصصة وأنواع التذاكر والتقديم |
| التفاعل والمحتوى | 🎉 | لوحة النجوم، قناة المميّز، وعدّاد الردود التلقائية ورد التفاعلات |
| الاقتصاد | 🏦 | البنك، المخالفات، الطيران |
| الدلائل | 📁 | قناة نشر توثيق العقوبات |
| السجلات | 📋 | ربط قناة سجل لكل نظام |
| الإعدادات | ⚙️ | البريفكس، رتبة الطاقم الأساسية |
| تشغيل الأنظمة | 🔀 | تفعيل/تعطيل أي نظام بضغطة |
| الحماية | 🔐 | تفعيل النظام العام + 7 قواعد فردية |
| الإحصائيات | 📈 | أرقام السيرفر العامة |
| المطور | 🧰 | حالة البوت، إعادة تحميل الأوامر، وضع الصيانة |
| مراقبة السيرفرات | 🛡️ | (للمطور) بحث بآيدي، مغادرة، حظر |
| الفحص الشامل | 🔍 | نفس تقرير `/فحص` من داخل اللوحة |
| المساعدة | ❓ | يوجّه لأمر `/مساعدة` الكامل |
| **الأنظمة المدنية** | 🏙️ | **نقطة دخول** لثلاثة أقسام فرعية (انظر أدناه) |

### 🏙️ الأنظمة المدنية (شاشة وسيطة)

اللوحة الرئيسية وصلت لحد ديسكورد (5 أزرار × 5 صفوف)، فبدل كسره تفتح **الأنظمة المدنية** شاشة فيها ثلاثة أقسام:

| القسم | يشمل |
|---|---|
| 🏙️ المدينة والسجن | كل ما في `RP.md` — السجن، الكلبشة، الرتب العسكرية، صور اللوحات |
| 🪪 الهوية الوطنية | قناة المراجعة، رتبة المواطن، مدة الصلاحية، قالب البطاقة، الطلبات المعلّقة |
| 🏛️ الحكومة | قنوات المجلس والتعاميم والتقاعد، رتبة المجلس، رتبة المتقاعد |

---

## لماذا بعض الأنظمة لها أوامرها المستقلة أيضًا

الأوامر التالية **بقيت كما هي** ولن تُدمج بالكامل داخل اللوحة، لأن كل عنصر فيها له **اسم مستقل** يصعب اختياره من قائمة محدودة:

- `/رد_تلقائي create` — كل قاعدة رد لها اسم وكلمات مفتاحية خاصة
- `/رد_تفاعل create` — نفس المبدأ
- `/دليل add` — كل دليل عقوبة منفصل بتفاصيله
- `/نوع_تذكرة` و `/تقديم` — لهما محرر تفاعلي كامل خاص بهما أصلاً

اللوحة تدير **الإعدادات العامة** لهذي الأنظمة (القنوات، التفعيل) وتعرض **عددها الحالي**، وتترك إدارة كل عنصر فردي لأمره المخصص.

---

## الصلاحيات

- فتح اللوحة: مستوى **الطاقم** فأعلى
- كل قسم له حد صلاحية خاص (مثلاً: مراقبة السيرفرات للمطور فقط)
- **الفحص يتم عند كل ضغطة زر أو اختيار من قائمة أو إرسال نافذة** — مو بس عند فتح اللوحة أول مرة. لو انخفض مستوى عضو أثناء تصفّحه للوحة، يُرفض من نفس اللحظة.

---

## ملاحظة تقنية

اللوحة **لا تحتوي منطقًا جديدًا**. كل شاشة تستدعي نفس الخدمات المستخدمة في الأوامر المباشرة (`app.ticketService`, `app.lifecycleService`, `app.economyService`...). يعني أي تعديل تسويه من اللوحة يحصل بنفس الطريقة اللي يحصل فيها من الأمر — لا فرق في النتيجة، فقط في طريقة الوصول.

# البنية المعمارية

هذا المستند يشرح **لماذا** بُني المشروع بهذا الشكل، لا فقط كيف. اقرأه قبل أي تعديل جوهري.

---

## 1) المبدأ الحاكم

> كل نظام مستقل برمجيًا، وكل الأنظمة مرتبطة بمنظومة إعدادات مركزية واحدة.

لا يستورد أي نظام نظامًا آخر مباشرة. التواصل يتم بطريقتين فقط:
- **حاوية التطبيق** (`Application`) تحقن الاعتماديات في كل نظام.
- **ناقل الأحداث** (`EventBus`) ينقل ما حدث دون أن يعرف المُرسِل من يستمع.

نتيجة عملية: نظام السجلات يسجّل كل عقوبة، لكن نظام الإدارة لا يعرف بوجود السجلات أصلًا. تستطيع حذف نظام السجلات كاملًا ولن ينكسر شيء.

---

## 2) الطبقات

```
┌──────────────────────────────────────────────┐
│  الوحدات (modules/)                          │  ← منطق كل نظام
│  moderation, tickets, staff, roles, security │
│  giveaways, polls, broadcast, panel, help    │
└───────────────────┬──────────────────────────┘
                    │ تستخدم فقط
┌───────────────────▼──────────────────────────┐
│  الأساس (core/)                              │
│  permissions │ commands │ errors │ i18n      │
│  events      │ logger   │ config             │
└───────────────────┬──────────────────────────┘
                    │ تستخدم فقط
┌───────────────────▼──────────────────────────┐
│  المستودعات (core/database/repositories/)    │  ← الطبقة الوحيدة التي تعرف SQL
└───────────────────┬──────────────────────────┘
                    │
┌───────────────────▼──────────────────────────┐
│  قاعدة البيانات (SQLite / better-sqlite3)    │
└──────────────────────────────────────────────┘
```

**قاعدة صارمة:** الطبقات الدنيا لا تعرف الطبقات العليا، ولا تعرف `discord.js`.

لهذا السبب تحديدًا انقسمت الأدوات إلى ملفين:
- `core/utils/common.js` — دوال نقية (تحليل المدة، استخراج الآيدي، القص). تستوردها المستودعات بأمان.
- `core/utils/helpers.js` — يضيف `buildEmbed` الذي يحتاج `discord.js`، ويعيد تصدير كل ما في `common.js`.

> هذا الانقسام لم يكن تنظيميًا فقط: قبله كان `ActivityRepository` يسحب `discord.js` عبر `helpers`، وهو خرق حقيقي للطبقات اكتُشف أثناء الاختبار.

---

## 3) لماذا SQLite؟

المواصفات طلبت قاعدة بيانات حقيقية لا ملفات JSON. الخيارات كانت PostgreSQL أو MongoDB أو SQLite.

**الاختيار: SQLite عبر `better-sqlite3`** للأسباب التالية:

| السبب | التفصيل |
|---|---|
| **معاملات ACID حقيقية** | وهي المطلب الأهم. منع Race Conditions مستحيل بدون معاملات ذرّية. |
| **لا خادم خارجي** | تعمل على أي استضافة بلا إعداد إضافي ولا تكلفة ولا سلسلة اتصال. |
| **متزامنة (synchronous)** | `better-sqlite3` متزامن، فلا توجد فجوة زمنية بين القراءة والكتابة تتسلل منها حالة سباق. |
| **أسرع من الشبكة** | لا زمن انتقال؛ استعلام محلي يقاس بالميكروثانية. |

**متى تنتقل إلى PostgreSQL؟** حين تحتاج عدة نسخ من البوت تكتب على نفس البيانات (Sharding مع حالة مشتركة). حتى ذلك الحين، SQLite أنسب وأسرع.

**كيف تنتقل؟** طبقة المستودعات معزولة تمامًا. تحتاج فقط:
1. استبدل `core/database/Database.js` باتصال Postgres.
2. أعد كتابة ملفات `repositories/*.js` بنفس أسماء الدوال والقيم المُرجَعة.
3. حوّل ملفات الهجرة في `migrations/` إلى صيغة Postgres.

**لا شيء خارج هذين المجلدين يحتاج تعديلًا** — لأن لا شيء خارجهما يكتب SQL.

---

## 4) منع Race Conditions

هذه أهم نقطة أمنية في المشروع. لم تُستخدم أقفال خارجية (Mutex/Lock) لأنها هشّة وتتسرب. بدلًا من ذلك: **الشرط داخل جملة `UPDATE` نفسها**.

### استلام التذكرة

```sql
UPDATE tickets SET claimed_by = ?, claimed_at = ?
WHERE channel_id = ? AND status = 'open' AND claimed_by IS NULL
```

المحرك ينفّذ هذه الجملة ذرّيًا. لو ضغط عشرة إداريين في نفس الميلي ثانية، واحد فقط سيحصل على `changes === 1`، والتسعة الباقون يحصلون على `0` ويُعرض لهم اسم المستلم الفعلي.

نفس النمط مطبّق على:

| العملية | الشرط الحارس |
|---|---|
| إغلاق التذكرة | `WHERE status = 'open'` |
| إعادة الفتح | `WHERE status = 'closed'` |
| إلغاء الاستلام | `WHERE claimed_by = ?` |
| إلغاء قضية | `WHERE active = 1` |
| إنهاء سحب | `WHERE status = 'active'` |
| خصم رصيد | `WHERE bank >= amount` |
| سداد مخالفة | `WHERE status = 'unpaid'` |
| قرار تقديم | `WHERE status = 'pending'` |
| حجز مقعد | `WHERE seats_taken < seats` + مفتاح أساسي |
| الدخول في سحب | `PRIMARY KEY (giveaway_id, user_id)` |

### ترقيم القضايا

عدّاد مستقل لكل سيرفر، والزيادة والإدراج داخل معاملة واحدة:

```js
db.transaction(() => {
  UPDATE case_counters SET last_number = last_number + 1 WHERE guild_id = ?
  SELECT last_number ...
  INSERT INTO cases (case_number, ...) ...
})
```

فيستحيل أن يحصل إجراءان متزامنان على نفس رقم القضية.

### سحب الفائزين

`markEnded()` هي البوابة: تُرجع `true` مرة واحدة فقط في عمر السحب. المؤقّت الدوري وزر "إنهاء الآن" قد يستدعيان الدالة في نفس اللحظة — واحد فقط يمر، فلا يُسحب فائزون مرتين.

---

## 5) محرك الصلاحيات

مصدر واحد للحقيقة: `core/permissions/PermissionService.js`.

**القاعدة التي لا تُكسر:**

> إخفاء الزر ليس حماية. إخفاء الأمر ليس حماية. الحماية هي الفحص من الخادم عند التنفيذ.

كل مسار دخول يمر بنفس المحرك:
- أوامر Slash → `CommandHandler._run()`
- أوامر Prefix و No-Prefix → نفس الدالة
- الأزرار والقوائم والمودالات → كل معالج يفحص بنفسه قبل أي إجراء

مثال حي: أزرار التذاكر فيها **30 فحص صلاحية** في `modules/tickets/interactions.js`، مع أن الأزرار أصلًا لا تظهر لغير المخوّل. الازدواجية مقصودة، لأن `customId` قابل للتزوير من العميل.

### دوال المحرك

| الدالة | ماذا تفعل |
|---|---|
| `resolveLevel(member)` | يحسب المستوى الفعلي (0–5) من المطور، مالك السيرفر، صلاحيات ديسكورد، ورتب السلم الإداري |
| `check(member, requirement)` | يفحص المستوى المطلوب + صلاحيات ديسكورد + قيد المطور |
| `canActOn(executor, target)` | فحص الهرمية: النفس، البوت، مالك السيرفر، الرتب المحمية، رتبة البوت |
| `botHas(guild, permissions)` | يتأكد أن البوت نفسه يملك ما يحتاجه قبل المحاولة |

**حدود واعية:** `canActOn` يرفض دائمًا أي هدف رتبته أعلى من رتبة البوت — **حتى لو كان الطالب مطورًا**. ديسكورد يفرض هذا على مستوى الـ API، ولا يوجد التفاف عليه. البوت يفشل بوضوح ورسالة مفهومة بدل أن يفشل صامتًا.

---

## 6) نظام الأوامر الموحّد

`CommandContext` يُخفي الفرق بين طرق الاستدعاء الثلاث، فيُكتب منطق الأمر مرة واحدة:

```js
const member = await ctx.getMember("user", 0);
// Slash  → interaction.options.getMember("user")
// Prefix → message.mentions أو args[0]
```

كل أمر يعرّف بياناته الوصفية بنفسه (`description`, `usage`, `arguments`, `examples`, `permissions`)، و`/help` **يُولَّد من هذه البيانات تلقائيًا**. إضافة أمر جديد تظهر في المساعدة بلا أي تعديل يدوي.

**نظام بدون Prefix** مقيّد بثلاث طبقات لتفادي التعارض مع المحادثة العادية: معطّل افتراضيًا، يعمل في قنوات محددة فقط، ويشترط مستوى صلاحية أدنى.

---

## 7) عزل السيرفرات (Multi-Guild)

كل سيرفر له صف مستقل في جدول `guilds` يحمل إعداداته كـ JSON. عند القراءة، تُدمج الإعدادات المحفوظة فوق `config/guild-defaults.json`.

**ثلاث ضمانات ضد تسرّب الإعدادات:**
1. لا توجد أي حالة عامة مشتركة — كل دالة تأخذ `guildId` صريحًا.
2. الكاش مفتاحه `guildId`، ويُبطَل فورًا عند أي كتابة.
3. كل جدول في قاعدة البيانات يحمل عمود `guild_id`، وكل استعلام يفلتر به.

اختُبر هذا: سيرفر ثانٍ يبدأ ترقيم قضاياه من `1` بينما الأول عند `2`، وتغيير بريفكس سيرفر لا يمس الآخر.

---

## 8) معالجة الأخطاء

لا يُسمح لأي خطأ بإسقاط البوت.

عند وقوع خطأ:
1. يُولَّد **رقم خطأ** من 8 خانات (مثل `A3F91C2B`).
2. يُسجَّل في قاعدة البيانات مع النظام والأمر والمستخدم والسيرفر والوقت والـ Stack.
3. يُرسل إمبيد مفصّل إلى **قناة سجل الأخطاء** إن كانت معرّفة.
4. يُعرض للمستخدم **رقم الخطأ فقط** — لا Stack Trace ولا تفاصيل داخلية.

المطور يستخدم `/dev errors id:A3F91C2B` لرؤية التفاصيل الكاملة. هذا يفصل بين ما يحتاجه المستخدم وما يحتاجه المطور، ويمنع تسريب بنية النظام.

المعالجات العامة (`unhandledRejection` و `uncaughtException`) مربوطة أيضًا، فحتى الأخطاء خارج سلسلة الأوامر تُلتقط ولا توقف العملية.

---

## 9) ناقل الأحداث

```
نظام الإدارة ──(MEMBER_BANNED)──► EventBus ──► نظام السجلات
                                          └──► نظام الإحصائيات
```

`emitSafe()` يضمن أن مستمعًا فاشلًا لا يُسقط البوت ولا يمنع بقية المستمعين — وهو مُختبَر فعليًا.

---

## 10) الأداء

- **الكاش:** إعدادات السيرفر تُخزَّن مؤقتًا 5 دقائق، وتُبطَل فورًا عند الكتابة.
- **المؤقتات:** فحص دوري واحد كل 15 ثانية للسحوبات والاستطلاعات، بدل مؤقّت لكل عنصر. العدد يبقى ثابتًا مهما كثرت السحوبات.
- **الإحصائيات:** نشاط الطاقم مُجمَّع **يوميًا** (`PRIMARY KEY (guild_id, user_id, day)`) بدل صف لكل رسالة، فلا يتضخم الجدول.
- **الفهارس:** موضوعة على كل عمود يُستخدم في الفلترة (`guild_id`, `target_id`, `status`, `ends_at`).
- **الإذاعة:** فاصل 1.2 ثانية بين الرسائل لاحترام حدود ديسكورد، مع عدّ النجاح والفشل.

---

## 11) إضافة نظام جديد

```
src/modules/اسم-النظام/
├── commands/           ← يُحمَّل تلقائيًا
├── interactions.js     ← اختياري: { prefix, handle(interaction, app) }
└── XService.js         ← اختياري: منطق النظام
```

الخطوات:
1. أنشئ المجلد وملف الأمر (يصدّر كائنًا أو مصفوفة كائنات).
2. لو احتجت جداول: أضف ملف هجرة جديدًا في `migrations/` (لا تعدّل القديم).
3. لو احتجت خدمة: سجّلها في `Application.init()`.
4. أضف نصوصك في `locales/ar.json`.

`CommandRegistry` و`InteractionRouter` يكتشفان الجديد تلقائيًا عند الإقلاع.

**اصطلاح `customId`:** `النظام:الإجراء:وسيط1:وسيط2` — الموجّه يوزّع حسب الجزء الأول.

---

## 12) ما اختُبر فعليًا

587 اختبارًا ناجحًا، صفر فاشل:

| الملف | العدد | يغطي |
|---|---|---|
| قاعدة البيانات والذرّية | 46 | الهجرات، ترقيم القضايا، الاستلام المتزامن، الإغلاق المزدوج، عزل السيرفرات، رفض حقن SQL |
| تحميل النظام | 24 | إقلاع التطبيق، 39 أمر ببيانات كاملة، صحة أوامر السلاش، مفاتيح الترجمة، معالج الأخطاء، ناقل الأحداث |
| محرك الصلاحيات | 24 | المستويات الستة، الهرمية، الرتب المحمية، قيد رتبة البوت |
| بناء الإمبيدات | 22 | الحفظ، المتغيرات، الأزرار والقوائم، مطابقة البريفكس، قص المحتوى الضخم |
| الاقتصاد | 68 | لا رصيد سالب، لا خلق مال، لا سداد مزدوج، لا بيع مقعد مرتين، استرجاع بعد فشل الدفع |
| أنواع التذاكر | 28 | القوائم المتداخلة، النماذج، حد التذاكر لكل نوع، إمبيد ترحيب مخصص |
| التقديمات والاختبار | 52 | الترقيم، القرار الذرّي، المهلة، تدفق الخاص، تصحيح الاختبار |
| محرّر التقديمات | 40 | تعديل الأسئلة فرديًا، الترتيب، الزخرفة، المنشن، حدود ديسكورد |
| دورة حياة الطاقم | 54 | حفظ/استعادة الرتب، الإنهاء التلقائي، التحذيرات المتصاعدة، القرار الذرّي |
| الردود والنشر | 53 | المطابقة العربية، الشروط، لوحة النجوم، النسخ الاحتياطي، الفحص الشامل |
| المراقبة والحماية | 44 | سجل السيرفرات، القائمة السوداء، تصعيد حارس الإساءة، استعادة الإغلاق بدقة |
| تحسينات التذاكر | 43 | الترقيم، حد الاستلام، مراحل الخمول، التقييم، تهريب HTML |
| التقارير والدلائل | 42 | الجدولة، منع التكرار اليومي، كشف غير النشطين، ترقيم الدلائل |
| الدمج الأخير | 37 | رد التفاعلات، التمييز الذرّي، بث الإصدارات، توقيع HMAC لـ GitHub |
| لوحة devpanel | 29 | فحص الصلاحية بكل تفاعل، 12 شاشة إدارية جديدة، حفظ الإعدادات فعليًا |

> **حدود الاختبار:** الشبكة كانت معطّلة أثناء البناء، فتعذّر `npm install`. اختُبر المنطق كاملًا عبر تشغيل حقيقي فوق `node:sqlite` المدمج وشيم يتحقق من صحة بناء أوامر السلاش وحدود ديسكورد. **لم يُختبر اتصال حي بخوادم ديسكورد** — هذا يتم عند أول تشغيل عندك.

# سياسة الأمان وحماية البوت

---

## الطلب الذي لم أنفّذه

طُلب أمر طوارئ **يحظر كل أعضاء سيرفر ويقفل رومه**، لحماية البوت من السيرفرات المسيئة.

**لم أبنه، ولن أبنيه.** والسبب ليس تحفظاً شكلياً:

### ١) لا يحمي البوت أصلاً

المشكلة المطروحة: سيرفر يغرق البوت بالأوامر فتُوقف الاستضافة. حظر ألف عضو في ذلك السيرفر **لا يوقف الإغراق** — العملية نفسها (ألف طلب حظر) هي أثقل حمل ممكن على البوت والاستضافة معاً. الحل الفعلي أن **يتوقف البوت عن الاستجابة ويغادر**، وهذا ما بنيته.

### ٢) يعاقب أبرياء

في سيرفر فيه 500 عضو، المسيء واحد أو اثنان. الباقون أعضاء عاديون يُحظرون بلا ذنب.

### ٣) يعرّض البوت للحذف

أدوات الحظر الجماعي (nuke) مخالفة صريحة لـ [سياسة مطوري ديسكورد](https://discord.com/developers/docs/policies-and-agreements/developer-policy). النتيجة المعتادة: **حذف التطبيق نهائياً**. طلبت حماية البوت، وهذا الأمر تحديداً أسرع طريق لخسارته.

### ٤) سلاح ذو حدين

أمر بهذه القوة، لو سُرّب توكن البوت يوماً، يتحول إلى أداة تدمير جاهزة ضد كل سيرفراتك. الأمر الذي لا يوجد لا يمكن إساءة استخدامه.

---

## ما بنيته بدلاً منه

يحل نفس المشكلة **فعلياً** وبلا أي من العيوب أعلاه:

### 🛡️ حارس الإساءة — الحماية الحقيقية للاستضافة

يعمل تلقائياً على كل أمر:

```
تجاوز الحد          →  تبريد صامت للسيرفر
تكرار التجاوز       →  حظر تلقائي + مغادرة + إشعار المطورين
```

| الإعداد | الافتراضي | المتغير |
|---|---|---|
| حد أوامر العضو/دقيقة | 25 | `ABUSE_USER_PER_MIN` |
| حد أوامر السيرفر/دقيقة | 80 | `ABUSE_GUILD_PER_MIN` |
| مدة التبريد | 5 دقائق | `ABUSE_COOLDOWN_MS` |
| إنذارات قبل الحظر | 3 | `ABUSE_STRIKES` |
| مغادرة تلقائية | مفعّلة | `ABUSE_AUTO_LEAVE` |

**الرفض صامت عمداً.** الرد على المُغرق يستهلك نفس الموارد التي نحميها، فنتجاهل بلا رد.

**المطورون معفيّون** حتى لا يُقفل عليك البوت أثناء الصيانة.

### 🚫 القائمة السوداء

```
/guilds blacklist id:<الآيدي> reason:<السبب>
```

- البوت يغادر فوراً
- يرفض العمل فيه حتى لو أُعيدت دعوته
- يغادر تلقائياً عند الإقلاع لو كان لا يزال فيه

```
/guilds unblacklist id:<الآيدي>
```

### 🌐 مراقبة السيرفرات

```
/guilds list sort:usage      الأكثر استخداماً للأوامر
/guilds list status:blacklisted
/guilds info id:<الآيدي>     تفاصيل + أحداث الإساءة آخر 7 أيام
/guilds stats                إجماليات
/guilds abuse                آخر أحداث الإساءة
/guilds leave id:<الآيدي>
```

كل سيرفر مسجّل: الاسم، المالك، الأعضاء، تاريخ الانضمام، عدد الأوامر، آخر نشاط. وتصلك رسالة خاصة عند انضمام البوت لأي سيرفر جديد.

### 🔒 الإغلاق الطارئ

```
/lockdown start reason:هجوم إغراق keep-open:#الإعلانات
/lockdown end
/lockdown status
```

يقفل الكتابة في كل القنوات مؤقتاً — إجراء مضاد للهجوم **قابل للعكس تماماً**:

- حالة كل قناة تُحفظ في قاعدة البيانات **قبل** القفل
- القناة المقفلة أصلاً لا تُلمس ولا تُسجّل
- عند الإنهاء تُستعاد الحالة بدقة: المفتوحة تُفتح، والموروثة ترجع موروثة
- تقدر تُبقي قناة واحدة مفتوحة للتواصل مع الأعضاء

**للأدمن في سيرفره فقط** — ليس أداة يستخدمها المطور على سيرفرات الآخرين.

---

## الفرق باختصار

| | الحظر الجماعي | ما بُني |
|---|---|---|
| يوقف الإغراق | ❌ يضاعفه | ✅ فوراً |
| يحمي الاستضافة | ❌ | ✅ |
| يؤذي أبرياء | ✅ | ❌ |
| مخالف لسياسة ديسكورد | ✅ | ❌ |
| قابل للعكس | ❌ نهائي | ✅ بالكامل |
| خطر لو سُرّب التوكن | كارثي | محدود |

---

## طبقات الأمان الأخرى في البوت

**محرك الصلاحيات المركزي** — كل زر وقائمة ومودال يُعيد الفحص من الخادم. إخفاء الزر ليس حماية.

**احترام تسلسل ديسكورد** — لا يتصرف البوت في رتبة أعلى منه، ولا يُمس مالك السيرفر، ولا تُوزّع رتب Administrator عبر الأزرار.

**عزل السيرفرات** — كل جدول يحمل `guild_id` وكل استعلام يفلتر به. لا تسرّب إعدادات بين السيرفرات.

**حماية قاعدة البيانات** — كل الاستعلامات معاملة، وأسماء الأعمدة والجداول من قوائم بيضاء ثابتة لا من مدخلات المستخدم.

**الأسرار** — من متغيرات البيئة فقط، و`.env` مستثنى من Git، و GitHub Actions يرفض أي رفع فيه سر مكشوف.

**الأخطاء** — رقم خطأ للمستخدم، والتفاصيل الكاملة للمطور فقط. لا تسريب Stack Trace.

---

## لو تعرّض بوتك لهجوم فعلي

1. `/guilds abuse` — من مصدر الإساءة؟
2. `/guilds blacklist id:<الآيدي> reason:<السبب>` — أوقفه فوراً
3. `/lockdown start` في سيرفرك لو الهجوم داخلي
4. `/dev maintenance enabled:true` — يوقف البوت عن الاستجابة للجميع عدا المطورين
5. لو تسرّب التوكن: **Reset Token** فوراً من بورتال ديسكورد، وحدّث `BOT_TOKEN` في Railway

---

## لو احتجت إجراءً أقوى

الأدوات المتاحة لك بلا مخالفة:

- **`/guilds blacklist`** — يمنع البوت نهائياً عن السيرفر المسيء
- **حظر التطبيق من ديسكورد** — بلّغ عن السيرفر عبر [Trust & Safety](https://support.discord.com/hc/requests/new) لو فيه نشاط مخالف. ديسكورد نفسه يملك صلاحية إغلاق السيرفر، وهذه الجهة الصحيحة
- **قفل الدعوات** — اجعل البوت خاصاً من بورتال ديسكورد (Bot → Public Bot = off) فلا يدعوه إلا أنت

هذي الطرق تحمي بوتك وتبقيه شغّالاً على المدى الطويل.

# خطة دمج نظام c2

هذا المستند يوثّق تحليل المشروع المرفوع (`c2-bot-main`) وخطة الدمج على مراحل.

---

## 1) نتيجة التحليل

| | c2-bot | بوتنا |
|---|---|---|
| أسطر الكود | 35,260 | 12,549 (قبل الدمج) |
| قاعدة البيانات | MongoDB + JSON احتياطي | SQLite (ACID) |
| **تعدد السيرفرات** | ❌ لا | ✅ نعم بعزل كامل |
| استلام التذكرة ذرّي | ❌ لا | ✅ نعم |
| محرك صلاحيات مركزي | ❌ لا | ✅ نعم |
| اختبارات آلية | 0 | 339 |
| الاستضافة | تحتاج MongoDB خارجي | بلا خدمة خارجية |

### لماذا لم أنسخ ملفاتهم كما هي

**السبب الأول — العزل بين السيرفرات.** دالة `getConfig()` عندهم ترجع إعدادات بلا `guildId`:

```js
function getConfig() {
  if (!configCache) configCache = loadConfigFromDisk();
  return cloneDeep(configCache);   // ← إعدادات واحدة لكل السيرفرات
}
```

20 ملف من أصل 24 في `src/utils` لا يعرفون `guildId` إطلاقاً. لو ركّبنا الكود كما هو، **إعدادات سيرفر تتسرّب لسيرفر آخر** — وهذا يكسر أهم ضمان في مشروعنا.

**السبب الثاني — حالة سباق حقيقية في الاستلام.** كودهم يقرأ ثم يكتب:

```js
if (!session.claimedBy) {          // ← قراءة
    ...
    updateSession(channel.id, { claimedBy: member.id });   // ← كتابة
}
```

لو ضغط إداريان في نفس اللحظة، الاثنان يمران. عندنا الشرط داخل جملة `UPDATE` نفسها فينجح واحد فقط.

**السبب الثالث — تعارض قواعد البيانات.** MongoDB يحتاج رابط اتصال خارجي وخدمة إضافية. مشروعنا يعمل بلا أي خدمة خارجية، وهذا مهم خاصة أن استضافتك عانت أصلاً من بناء المكتبات.

**القرار:** أعيد كتابة أنظمتهم المفيدة داخل معماريتنا. هذا يحفظ الفكرة ويُسقط العيوب.

---

## 2) ما أُنجز — المرحلة 1 (v8)

### 🌴 نظام الإجازات
- طلب بمدة وسبب → مراجعة بأزرار → قبول أو رفض
- عند القبول: **تُحفظ الرتب المسحوبة في السجل** وتُستعاد بدقة عند الانتهاء
- إنهاء تلقائي عند انتهاء المدة، أو يدوي بـ `/leave end`
- رتب معفاة من السحب، ورتبة إجازة تُعطى وتُسحب تلقائياً

> **تحسين على الأصل:** كودهم يستعيد الرتب من قائمة الإعدادات وقت العودة. لو غيّر الأدمن الإعدادات أثناء الإجازة، العضو يرجع برتب خاطئة. عندنا الرتب تُحفظ **لحظة السحب** في سجل الطلب، فالاستعادة دقيقة دائماً.

### 📤 نظام الاستقالات
- طلب بسبب → مراجعة → عند القبول تُسحب رتب الطاقم وتُحفظ للسجل
- رتبة "مستقيل" اختيارية تُعطى بعد القبول

### 🚨 البلاغات على الإداريين
- بلاغ بالسبب والوقت والمكان والشهود و**حتى 10 أدلة**
- تحذيرات متصاعدة تلقائياً: كل بلاغ مقبول يرفع الدرجة (1 → 2 → 3) ويعطي الرتبة المقابلة
- عند بلوغ الدرجة الأخيرة تُبلَّغ الإدارة العليا في قناتها
- مهلة بين البلاغات لمنع الإغراق
- الأدلة تقبل روابط `https` فقط

**الأوامر:** `/leave` • `/resign` • `/report`
**الاختبارات:** 54 جديدة، صفر فاشلة

---

## 3) ما تبقّى — المراحل القادمة

مرتّبة حسب القيمة مقابل الجهد:

## ✅ الدمج مكتمل بالكامل (v13)

كل نظام مفيد في c2-bot-main أصبح جزءًا من هذا البوت، مُعاد كتابته بعزل كامل لكل سيرفر وقرارات ذرّية واختبارات. الملخص الكامل بالأسفل.

### ~~المرحلة 2 — الرد التلقائي~~ ✅ منجزة في v9
- **Auto-Reply**: 4 أنواع مطابقة + توحيد النص العربي + شروط قنوات ورتب وتبريد
- **Starboard**: نُقلت من المرحلة 4 لأنها صغيرة ومستقلة
- **تحسين على الأصل:** أضفنا توحيد الهمزات والتشكيل والتاء المربوطة — كودهم يطابق حرفيًا فيفشل مع «أحمد» مقابل «احمد»

### ~~المرحلة 3 — تحسينات التذاكر~~ ✅ منجزة (v11 + v12)
- ✅ تقييم بالنجوم + قناة تقييمات + متوسطات لكل إداري
- ✅ الإغلاق التلقائي على مرحلتين
- ✅ ترانسكربت HTML بتصميم عربي
- ✅ حد التذاكر المستلمة
- ✅ ترقيم التذاكر (لم يكن في خطتنا الأصلية)
- ✅ التقارير الدورية وتنبيه انخفاض النشاط

> **تحسين على الأصل:** ترانسكربتهم يكتب محتوى الرسائل في HTML بلا تهريب، فرسالة فيها `<script>` تُنفَّذ عند فتح الملف. عندنا كل المحتوى مهرَّب — مُختبَر.

### المرحلة 4 — أنظمة المحتوى — ✅ شبه منجزة
- ✅ **الدلائل** — نُفّذت في v12 وأوسع من الأصل (ربط بالقضايا، 10 روابط، ترقيم)
- ✅ ~~Starboard~~ في v9
- ⏳ **Featured** (543): منشورات مميزة — الوحيد المتبقي من هذه المرحلة

### ~~المرحلة 5 — أدوات المطور~~ ✅ منجزة في v13
- ✅ **Changelog + Version Notifier** — بث تلقائي لكل سيرفر مشترك
- ✅ **GitHub Webhook** — استقبال حقيقي بتحقق HMAC، أوسع من الأصل (4 أنواع أحداث بإمبيدات منسّقة)
- **Error Logger المتقدم (290)** — لم يُنقل: نظام الأخطاء الحالي (`/dev errors`) يغطي نفس الحاجة بمعمارية أنظف

> **ما لن أنقله:** لوحاتهم الأربع (`master-panel` • `admin-panel` • `admin-board` • `botPanel` = 5,463 سطر) لأنها بديل عن `/panel` عندنا وتؤدي نفس الغرض بمعمارية أضعف. و`embeds.js` (1,781) لأن محرّرنا يغطيه ويزيد عليه.

---

## 4) ما تحتاجه لتشغيل الدمج

**لا شيء إضافي.** الأنظمة المدموجة تعمل على نفس البنية الحالية:
- بلا MongoDB ولا أي خدمة خارجية
- بلا مكتبات جديدة
- الجداول تُنشأ تلقائياً عند أول تشغيل (هجرة `008_lifecycle.sql`)

### الإعداد بعد التحديث

من `/panel` ← **تشغيل الأنظمة** فعّل: الإجازات • الاستقالات • البلاغات
ثم من `/panel` ← **السجلات** اربط قنواتها.

باقي الإعدادات (قنوات الطلبات، رتبة الإجازة، الرتب المسحوبة، رتب التحذير) في `config/guild-defaults.json` تحت `leave` و `resign` و `reports`.

**للمرحلة 3 فقط** ستحتاج لاحقاً مكتبة اختيارية للترانسكربت:
```
npm install discord-html-transcripts
```
وإن لم تُثبَّت، يبقى الترانسكربت النصي الحالي شغّالاً.

---

## 5) الملفات المرجعية

المشروع المرفوع محفوظ كمرجع أثناء الدمج، ولم يُدمج منه أي ملف حرفياً. كل ما نُقل أُعيدت كتابته بـ:
- عزل كامل لكل سيرفر
- قرارات ذرّية في قاعدة البيانات
- فحص صلاحيات من الخادم عند كل ضغطة
- اختبارات تغطي الحالات الحرجة

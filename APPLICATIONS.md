# دليل التقديمات واختبار التفعيل

يبني بالضبط اللي في صورك: تقديم على العصابات/الداخلية/الإعلام، يُنشر للمراجعة، والإداري يضغط قبول أو رفض.

---

## نظام التقديمات

### 1) أنشئ نوع التقديم

```
/application create name:العصابات label:طلب تقديم - العصابات review:#مراجعة_التقديمات role:@عصابة cooldown:7d
```

| الخيار | وش يسوي |
|---|---|
| `name` | الاسم المختصر اللي تكتبه في اللوحات (بدون مسافات) |
| `label` | العنوان الظاهر في الطلب |
| `review` | القناة اللي تُنشر فيها الطلبات للمراجعة **(مطلوب)** |
| `role` | الرتبة اللي تُعطى تلقائياً عند القبول |
| `remove-role` | رتبة تُسحب عند القبول (مثل رتبة زائر) |
| `mode` | `modal` نافذة سريعة، أو `dm` أسئلة في الخاص |
| `cooldown` | مهلة إعادة التقديم بعد الرفض، مثل `7d` |
| `category` | فئة رئيسية تُجمَّع تحتها الأنواع، مثل `وزارة الداخلية` (اختياري) |

> بعد الإنشاء، **كل شي تعدّله بالمحرّر التفاعلي** — مو محتاج تحفظ أوامر:
> ```
> /application edit name:الشرطة
> ```

### 2) أضف الأسئلة

```
/application form name:العصابات q1:الاسم q2:العمر q3:الخبرات q4:اسم العصابة المقدم عليها q5:سبب التقديم numeric:2
```

- حتى **10 أسئلة**
- `numeric:2` يخلي السؤال الثاني يقبل أرقام فقط
- `long:3,5` يخلي السؤالين طويلين
- `image:6` يخلي السؤال السادس يطلب صورة

> **مهم:** لو الأسئلة أكثر من 5 أو فيها صورة، البوت ينتقل تلقائياً لجمعها **في الخاص** لأن نافذة ديسكورد محدودة بـ5 حقول ولا تقبل صور.

### 3) اربطه بزر أو قائمة

في محرّر الإمبيد، خانة الإجراء:

```
apply:العصابات
```

أو قائمة فيها كل التقديمات:

```
تقديم العصابات | للانضمام لعصابة | apply:العصابات
تقديم الداخلية | وزارة الداخلية | apply:الداخلية
تقديم إعلامي | وزارة الإعلام | apply:الاعلام
```

---

## المحرّر التفاعلي

```
/application edit name:الشرطة
```

تنفتح لك لوحة فيها **معاينة حية** لشكل الطلب، وتحتها الأزرار:

| الزر | وش تعدّل فيه |
|---|---|
| 📝 **النصوص** | الاسم الظاهر • الوصف • رسالة القبول • رسالة الرفض |
| 🎨 **الزخرفة** | عنوان الطلب • اللون • صورة كبيرة • صورة مصغّرة • الفوتر |
| ❓ **الأسئلة** | إضافة • تعديل • حذف • إعادة ترتيب — سؤال سؤال |
| ⚙️ **الإعدادات** | طريقة الجمع • مهلة إعادة التقديم • الإيموجي |
| 📥 **قناة المراجعة** | تختارها من قائمة، والبوت يتأكد أنه يقدر يرسل فيها |
| 🎖️ **رتبة القبول** | يتأكد أن رتبة البوت أعلى منها قبل الحفظ |
| ➖ **رتبة تُسحب** | رتبة تُشال عند القبول (مثل رتبة زائر) |
| 🔔 **المنشن** | حتى 5 رتب + `@everyone` + `@here` |
| 🔒 **فتح/إغلاق** | إيقاف التقديم مؤقتاً بضغطة |
| 📤 **نشر لوحة التقديم** | ينشر إمبيد بزر تقديم جاهز في أي قناة |
| 🗑️ **حذف النوع** | بتأكيد |

### إضافة أنواع جديدة

نفس الطريقة لأي قسم تبيه:

```
/application create name:الشرطة label:تقديم - الشرطة review:#مراجعة role:@شرطي
/application create name:الاسعاف label:تقديم - الإسعاف review:#مراجعة role:@مسعف
/application create name:القضاء label:تقديم - القضاء review:#مراجعة role:@قاضي
```

ثم `/application edit name:الشرطة` وزخرفه كيف ما تبي.

---

## الزخرفة والمتغيرات

في **🎨 الزخرفة** تقدر تستخدم هذي المتغيرات:

| المتغير | يطلع |
|---|---|
| `{type}` | اسم التقديم |
| `{number}` | رقم الطلب |
| `{user}` | اسم مقدّم الطلب |
| `{server}` | اسم السيرفر |

مثال — عنوان: `طلب انضمام — {type}` وفوتر: `رقم الطلب {number} • {server}`

**إيموجي الحقول** يغيّر الرمز اللي قدام كل سؤال في الطلب. الافتراضي `👤` زي صورك، وتقدر تخليه `🔹` أو أي شي أو تفضيه.

> **ملاحظة:** لون الانتظار قابل للتخصيص، لكن **القبول أخضر والرفض أحمر دائماً** — عشان الإداري يميّز الحالة بلمحة بدون ما يقرأ.

---

## المنشن

من زر **🔔 المنشن**:
- اختر حتى **5 رتب** من القائمة
- أزرار `@everyone` و `@here` تشتغل بالتبديل (ضغطة تضيف، ضغطة تشيل)
- **مسح الكل** يلغي كل المنشنات

المنشن ينرسل مع رسالة الطلب في قناة المراجعة.

---

## شكل الطلب عند المراجعة

يُنشر في قناة المراجعة إمبيد فيه:

```
طلب تقديم — العصابات
👤 العضو: @أحمد
👤 الاسم: لبش علي
👤 العمر: 14
👤 الخبرات: كل شي
👤 اسم العصابة المقدم عليها: سكراب
👤 سبب التقديم: للفساد

الحالة
⏳ بانتظار المراجعة

طلب رقم #12
```

وتحته ثلاثة أزرار: **✅ قبول** • **❌ رفض** • **📝 رفض بسبب**

بعد القرار تتحدّث نفس الرسالة:

```
الحالة
✅ تم قبول الطلب
@أبو أحمد • 28/08/2026 1:09 م
```

---

## وش يصير عند القبول

1. تُعطى رتبة القبول للعضو
2. تُسحب الرتبة المحددة في `remove-role` إن وُجدت
3. تتحدّث رسالة الطلب لتعرض القرار واسم المراجع ووقته
4. يوصل العضو إشعار في الخاص

### تخصيص رسالة القبول والرفض

من المحرّر ← **📝 النصوص**، أو مباشرة:
```
/application set name:العصابات field:رسالة القبول value:تشعرك لجنة القبول بقبولك في {type}. توجّه إلى #الدعم لاستلام رتبتك.
```

المتغيرات: `{user}` • `{server}` • `{type}`

---

## متابعة الطلبات

```
/application pending          الطلبات المعلّقة
/application show number:12   عرض طلب برقمه
/application list             الأنواع + إحصائيات
```

إغلاق التقديم مؤقتاً: من المحرّر ← زر **🔒 إغلاق التقديم**

أو مباشرة:
```
/application set name:العصابات field:مفتوح للتقديم value:لا
```

---

## لوحة التصفّح الهرمية (فئة ← نوع ← استمارة)

لو عندك عدة أقسام كبيرة، كل قسم له وظائف فرعية (مثل: وزارة الداخلية ← شرطي / محقق / قاضٍ)، صنّف الأنواع تحت فئات:

```
/application create name:داخلية_شرطي label:شرطي review:#مراجعة role:@شرطي category:وزارة الداخلية
/application create name:داخلية_محقق label:محقق review:#مراجعة role:@محقق category:وزارة الداخلية
/application create name:عصابات_عضو label:عضو عصابة review:#مراجعة role:@عصابة category:العصابات
```

ثم انشر لوحة واحدة تجمع كل شي:

**/لوحة ← الأنظمة والإعدادات ← 📤 نشر اللوحات ← لوحة التقديمات** ثم اختر القناة (مثل `#تقديم-الوظائف`).

العضو يشوف قائمة فئات (وزارة الداخلية، العصابات...)، يختار فئة فتطلع له قائمة ثانية بالوظائف الفرعية، يختار وظيفة فتُفتح له نفس نموذج التقديم العادي (نافذة أو أسئلة بالخاص، حسب إعداد النوع).

**أنواع بلا فئة** (ما حطيت لها `category`) تظهر مباشرة في القائمة الأولى، جنب الفئات — مفيد لتقديم مستقل زي "استقالة" ما يحتاج تصنيف.

فئة ما فيها أي نوع مفعّل تختفي تلقائياً من القائمة، فما يشوف العضو خيارات ميتة.

لتغيير فئة نوع موجود:
```
/application set name:داخلية_شرطي field:الفئة الرئيسية value:وزارة الداخلية الجديدة
```

---

## الأسئلة في الخاص

لما يكون الوضع `dm`، البوت يراسل العضو:

```
📋 التقديم على تقديم الأحوال

سأطرح عليك بعض الأسئلة، أجب على كل سؤال برسالة منفصلة في هذا الخاص.
اكتب `إلغاء` في أي وقت للخروج.

سؤال 1/4 — ما هو الاسم الكامل لشخصيتك؟
```

- كل إجابة برسالة مستقلة
- السؤال الرقمي يرفض النص ويطلب أرقام
- سؤال الصورة يطلب مرفق صورة
- `إلغاء` يخرج من النموذج
- المهلة **15 دقيقة**، وبعدها تنتهي الجلسة تلقائياً

> لو خاص العضو مقفل، يطلع له تنبيه واضح يطلب منه فتحه.

---

## اختبار التفعيل

### 1) أضف الأسئلة

```
/quiz add question:الرول بلاي هو تقمص الشخصية؟ correct:نعم
/quiz add question:يجوز تخريب السيرفر؟ correct:لا
/quiz add question:يجب احترام الإدارة؟ correct:نعم
```

حتى **50 سؤال**.

### 2) اضبط الإعدادات

```
/quiz settings enabled:true pass-score:8 role:@مفعل results:#نتائج_التفعيل
```

- `pass-score` عدد الإجابات الصحيحة المطلوبة (الافتراضي 70% من الأسئلة)
- `role` الرتبة اللي تُعطى تلقائياً عند النجاح
- `results` قناة نشر النتائج

### 3) انشر اللوحة

**/لوحة ← نشر اللوحات ← لوحة اختبار التفعيل** ثم اختر القناة (مثل `#التفعيل`).

أو اربطها بزر في لوحتك المصممة — الإجراء: `quiz`

### شكل الاختبار

سؤال واحد في كل مرة بزرَّي **نعم** و **لا**، والرد مخفي للعضو وحده. وبالنهاية:

```
نتيجة اختبار التفعيل
العضو: @سعيد
الإجابات الصحيحة: 9
الإجابات الخاطئة: 1
المطلوب للنجاح: 8 من 10

✅ | مبروك، لقد اجتزت اختبار التفعيل!
```

النتيجة تُنشر في قناة النتائج، والرتبة تُعطى فوراً.

> أزرار الاختبار مربوطة بصاحبها، فما ينفع أحد يجاوب نيابة عن غيره.
> ومن اجتاز الاختبار ما يقدر يعيده.

---

## أمثلة جاهزة

### لوحة تقديمات الوزارات

```
/application create name:الداخلية label:طلب تقديم - وزارة الداخلية review:#مراجعة role:@داخلية
/application form name:الداخلية q1:الاسم q2:العمر q3:الخبرات q4:سبب التقديم numeric:2

/embed create name:لوحة_التقديمات
```
قائمة بالخيارات:
```
وزارة الداخلية | للانضمام للأمن | apply:الداخلية
العصابات | للانضمام لعصابة | apply:العصابات
الإعلام | صحفي أو مصور | apply:الاعلام
```

### تقديم الأحوال بصورة الشخصية

```
/application create name:الاحوال label:تقديم الأحوال review:#مراجعة_الأحوال role:@مواطن mode:dm
/application form name:الاحوال q1:ما هو الاسم الكامل لشخصيتك؟ q2:كم عمر شخصيتك؟ q3:ما هو تاريخ ميلاد شخصيتك؟ q4:ما هو مكان ميلاد شخصيتك؟ q5:أرسل صورة شخصيتك numeric:2 image:5
```

الأسئلة تنرسل في الخاص واحد واحد، والصورة تظهر في الطلب عند المراجعة.

---

## حدود وحمايات

- **50 نوع تقديم** • **20 سؤال لكل تقديم** • **50 سؤال اختبار** لكل سيرفر
- طلب واحد معلّق لكل عضو في كل نوع
- القرار ذرّي — لو ضغط إداريان في نفس اللحظة، واحد فقط يمر والثاني يشوف تنبيه
- أزرار المراجعة للطاقم فقط، وإعادة الفتح للأدمن فقط، والفحص من الخادم عند كل ضغطة
- إعطاء الرتبة يحترم تسلسل ديسكورد، ولو رتبة البوت أقل يظهر تنبيه واضح للإداري
- جلسات الخاص في الذاكرة وتنتهي بعد 15 دقيقة — إعادة تشغيل البوت تلغيها والعضو يبدأ من جديد

---

## أخطاء شائعة

| المشكلة | الحل |
|---|---|
| `خاصك مقفل` | إعدادات الخصوصية في السيرفر ← السماح بالرسائل الخاصة |
| الطلب ما ينشر | حدّد قناة المراجعة، وتأكد أن البوت يقدر يرسل فيها |
| الرتبة ما تنعطى | ارفع رتبة البوت فوق رتبة القبول |
| `عندك طلب قيد المراجعة` | خلّص الطلب السابق (قبول أو رفض) أولاً |
| الأسئلة تجي في الخاص مع إني أبيها نافذة | قللها لـ5 أو أقل وشِل سؤال الصورة |
| زر الاختبار ما يشتغل | `/quiz settings enabled:true` وأضف أسئلة أولاً |

---

**للوحات والقوائم المتداخلة:** `PANELS.md` • **للمحرّر:** `BUILDER.md` • **للبنك:** `ECONOMY.md`

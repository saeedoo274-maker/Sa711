# دليل بناء الإمبيدات والأوامر

كل شي هنا يتسوّى **من داخل ديسكورد**. ما تحتاج تفتح أي ملف ولا تعدّل أي كود.

---

## مثال كامل: لوحة دعم فني بأزرار

هذي بالضبط مثل اللوحات اللي تشوفها في السيرفرات الكبيرة.

### 1) سوِّ الإمبيدات اللي راح تفتحها الأزرار

```
/embed create name:قوانين_الدعم
```

راح ينفتح لك **المحرّر** مباشرة. اضغط **📝 النصوص** واكتب:

- **العنوان:** `قوانين الدعم الفني`
- **الوصف:** `احترم الإداري • لا تزعج بالمنشن • اكتب مشكلتك بالتفصيل`

اضغط **🎨 اللون** وحط `#FFD700`، ثم **🖼️ الصور** وحط رابط البانر.

### 2) سوِّ اللوحة الرئيسية

```
/embed create name:لوحة_الدعم
```

من **📝 النصوص**:
- **العنوان:** `الدعم الفني`
- **الوصف:** `مرحباً {user} في {server} — اختر الخدمة المناسبة`
- **نص فوق الإمبيد:** `@everyone` (لو تبي منشن)

### 3) ركّب الأزرار

اضغط **🔘 إضافة زر** واملأ:

| الحقل | القيمة |
|---|---|
| نص الزر | `تذكرة مساعدة` |
| إيموجي | `🎫` |
| اللون | `success` |
| الإجراء | `قوانين_الدعم` |
| الرد للجميع؟ | `لا` |

كرّر لكل زر تبيه.

### 4) انشرها

اضغط **📤 إرسال لقناة** واختر القناة. خلاص، اللوحة شغّالة.

---

## أنواع الإجراءات

في خانة **الإجراء** تقدر تكتب أربع صيغ:

| تكتب | يصير |
|---|---|
| `اسم_الإمبيد` | يفتح إمبيد ثاني |
| `link:https://discord.gg/xxx` | زر رابط خارجي |
| `role:123456789012345678` | يعطي/يسحب رتبة |
| `text:أهلاً فيك` | يرد بنص مباشر |
| `ticket:اسم_النوع` | يفتح تذكرة بنموذج أسئلة وكاتيغوري خاصة |
| `apply:اسم_النوع` | يبدأ تقديمًا يُنشر للمراجعة بأزرار قبول ورفض |
| `quiz` | يبدأ اختبار التفعيل |
| `sys:bank:balance` | يشغّل عملية حقيقية في البنك أو المخالفات أو الطيران |
| `sys:flight:join:MT-101` | يحجز مقعدًا في **رحلة محددة** مباشرة بلا قائمة اختيار |

> إجراءات `sys:` هي اللي تخلي لوحاتك تشتغل فعلياً بدل ما تعرض إمبيدات فقط.
> القائمة الكاملة في **ECONOMY.md**.
> ولبناء قوائم متداخلة تفتح تذاكر: **PANELS.md**.

---

## 📖 ما تحتاج تحفظ الصيغ

داخل المحرّر فيه زر **📖 الإجراءات المتاحة** — يعرض لك **كل ما هو موجود فعليًا في سيرفرك الآن**، جاهزًا للنسخ:

- أنواع التذاكر عندك بأسمائها (`ticket:دعم`)
- أنواع التقديم (`apply:شرطة`)
- **الرحلات المفتوحة برموزها** (`sys:flight:join:MT-101`)
- إمبيداتك الأخرى
- كل إجراءات الأنظمة مع شرح كل واحد

يعني تفتح الزر، تنسخ الإجراء، وتلصقه — بلا حفظ ولا تخمين.

---

## ✏️ تعديل زر أو قائمة بعد إنشائها

قبل، لو غلطت في زر كان لازم تحذف كل المكوّنات وتعيدها. الآن فيه زر **✏️ تعديل مكوّن**:

1. اضغط **تعديل مكوّن**
2. اختر الزر أو القائمة من القائمة (تشوف اسم كل واحد ووش يسوي)
3. تفتح لك نافذة **معبّأة بقيمه الحالية** — تعدّل اللي تبيه وتحفظ

نفس الشي للقوائم: خياراتها تطلع لك مكتوبة بالصيغة الجاهزة، تعدّل سطر أو تضيف سطر وتحفظ.

**التحقق يشتغل عند التعديل زي الإنشاء تمامًا** — لو كتبت رمز رحلة غير موجود أو اسم تذكرة غلط، يرفض ويوضّح لك السبب، والمكوّن القديم يبقى كما هو بلا تخريب.

---

## 🖼️ رفع صورة مباشرة (بلا رابط خارجي)

خانة "الصور" في المحرّر تطلب **رابط** فقط — لأن نوافذ الإدخال في ديسكورد ما تقبل رفع ملفات إطلاقًا، فقط نصوصًا. الحل: أمر منفصل يرفع الملف مباشرة من جهازك.

```
/ايمبد صورة name:الدعم_الفني file:<ارفع الصورة من جهازك> type:كبيرة
```

| الخيار | الشرح |
|---|---|
| `name` | اسم الإمبيد الموجود |
| `file` | الصورة نفسها — ترفعها من معرض جهازك مباشرة |
| `type` | **كبيرة** (تحت الوصف) أو **مصغّرة** (أعلى يمين). الافتراضي: كبيرة |

يتحقق الأمر أن الملف صورة فعلية قبل الحفظ. بعدها استخدم `/embed sync name:الدعم_الفني` لتحديث أي رسالة سبق ونشرتها بهذا الإمبيد.

> **ملاحظة:** الصورة تُخزَّن كرابط CDN من ديسكورد نفسه — تماشيًا مع نفس الحقل الذي يقرأه خيار "الصورة الكبيرة (رابط)" في المودال، فتقدر تستخدم الطريقتين بالتبادل على نفس الإمبيد.

---

## القوائم المنسدلة

اضغط **📑 إضافة قائمة**، وفي خانة الخيارات اكتب **سطر لكل خيار** بهذي الصيغة:

```
الاسم | الوصف | اسم الإمبيد
```

مثال — قائمة خدمات بنكية:

```
كشف حساب | لكشف حسابك الشخصي | كشف_الحساب
تحويل مبلغ | إرسال حوالة مالية | تحويل_مبلغ
إيداع مبلغ | إيداع في حسابك | ايداع_مبلغ
سحب مبلغ | سحب الأموال | سحب_مبلغ
```

كل خيار لازم يكون له إمبيد موجود مسبقاً. لو خيار فيه غلط، البوت يضيف الباقي ويقول لك أي خيار سقط وليش.

---

## الأوامر المخصصة

تبي أمر زي `-تفعيل` يطلع اللوحة؟

```
/command create name:تفعيل embed:لوحة_الدعم prefix:-
```

خلاص. أي واحد يكتب `-تفعيل` تطلع له اللوحة.

### الخيارات المتاحة

| الخيار | وش يسوي |
|---|---|
| `prefix` | بريفكس خاص لهذا الأمر (`-` أو `!` أو `؟`). فاضي = بريفكس السيرفر |
| `level` | أقل مستوى يقدر يستخدمه: الجميع / الطاقم / المشرفون / الأدمن |
| `ephemeral` | الرد يشوفه صاحب الأمر بس |
| `delete-trigger` | يحذف رسالة الأمر بعد الرد |
| `mentions` | يسمح بمنشن `@everyone` والرتب |
| `content` | رد نصي بدل الإمبيد |

### تعديل أمر موجود

```
/command edit name:تفعيل field:البريفكس value:!
/command edit name:تفعيل field:مستوى الصلاحية value:2
/command edit name:تفعيل field:الإمبيد المرتبط value:لوحة_ثانية
```

---

## تعديل لوحة منشورة

هذي أهم ميزة: **عدّل مرة واحدة، ينطبق على كل مكان**.

1. `/embed edit name:لوحة_الدعم`
2. عدّل اللي تبيه
3. اضغط **🔄 تحديث المرسل**

كل الرسائل اللي أرسلتها من هذا الإمبيد تتحدّث فوراً — حتى لو منشورة في عشر قنوات.

> البوت يتتبّع الرسائل تلقائياً، وأي رسالة انحذفت يدوياً يشيلها من التتبّع بدون ما يزعجك.

---

## الشكل الحديث: الأزرار داخل الحاوية

افتراضيًا الإمبيد يطلع بالشكل **التقليدي** — الأزرار منفصلة تحته. لو تبي الشكل اللي في السيرفرات الحديثة (الأزرار **داخل** الحاوية الملوّنة نفسها):

1. افتح المحرّر: `/embed edit name:اسم_الإمبيد`
2. اضغط زر **الشكل** — يتحوّل من 📋 تقليدي إلى 🆕 حديث
3. اضغط **معاينة حية 👁️** لتشوف الشكل النهائي بالضبط قبل النشر

### الفرق

| | تقليدي 📋 | حديث 🆕 |
|---|---|---|
| الأزرار | منفصلة تحت الإمبيد | **داخل** الحاوية |
| الصور | صورة كبيرة + مصغّرة | معرض وسائط مدمج |
| العنوان والحقول | حقول إمبيد منفصلة | نص منسّق داخل الحاوية |
| الشريط الملوّن | نعم | نعم |

**كل شي ثاني يشتغل زي ما هو:** المتغيرات، الحقول، الفوتر، الأزرار اللي تفتح إمبيدات ثانية، و«تحديث المرسل».

> **ملاحظة:** لو ضغطت **تحديث المرسل** بعد تغيير الشكل، ديسكورد ما يسمح بتحويل رسالة منشورة من نمط لنمط. الحل: احذف الرسالة القديمة وانشر الإمبيد من جديد بـ **إرسال لقناة**.

---

## المتغيرات

تقدر تحطها في أي نص داخل الإمبيد:

| المتغير | يطلع |
|---|---|
| `{user}` | منشن العضو |
| `{username}` | اسم العضو بدون منشن |
| `{server}` | اسم السيرفر |
| `{membercount}` | عدد الأعضاء |

---

## بقية أوامر الإمبيد

```
/embed list                              عرض كل الإمبيدات
/embed clone name:أ newname:ب            نسخة من إمبيد
/embed rename name:أ newname:ب           تغيير الاسم
/embed send name:أ channel:#قناة         إرسال بدون فتح المحرّر
/embed sync name:أ                       تحديث الرسائل المنشورة
```

---

## حدود وحمايات

هذي مقصودة، وتحميك من كسر اللوحات:

- **100 إمبيد** و **200 أمر مخصص** لكل سيرفر
- الأزرار والقوائم: ديسكورد يسمح بـ **5 صفوف**، و**5 أزرار** بالصف، و**25 خيار** بالقائمة. البوت يقص الزايد تلقائياً بدل ما يفشل الإرسال
- النصوص تتقص على حدود ديسكورد تلقائياً، فما راح تشوف خطأ إرسال أبداً
- الروابط لازم تبدأ بـ `https://`
- **ما تقدر توزّع رتبة فيها صلاحية Administrator** عبر الأزرار
- ما تقدر توزّع رتبة أعلى من رتبة البوت
- المنشن الجماعي معطّل افتراضياً، وتفعّله بخيار `mentions`
- إنشاء وتعديل الإمبيدات **للأدمن فقط**، والفحص يتم من الخادم عند كل ضغطة زر — مو بإخفاء الأزرار

---

## أخطاء شائعة

| المشكلة | السبب |
|---|---|
| الأمر ما يشتغل | تأكد من البريفكس بـ `/command list` |
| الزر يقول "لم يعد فعّالاً" | الإمبيد المرتبط انحذف. عدّل الزر أو أعد إنشاء الإمبيد |
| اللوحة ما تحدّثت | اضغط **🔄 تحديث المرسل** بعد التعديل |
| الصورة ما تظهر | الرابط لازم يبدأ بـ `https://` وينتهي بامتداد صورة |
| الاسم مرفوض | بدون مسافات — استخدم `_` بدلها |

# دليل لوحات الخدمات والقوائم المتداخلة

هذا الدليل يبني بالضبط النوع اللي في صورك: لوحة رئيسية فيها قائمة، كل خيار يفتح قائمة ثانية، وكل خيار في القائمة الثانية يفتح تذكرة بنموذج أسئلة خاص.

---

## الفكرة في ثلاث كلمات

**إمبيد ← قائمة ← تذكرة**

- كل قائمة تعيش داخل إمبيد
- خيار القائمة يقدر يفتح **إمبيد ثاني** (وفيه قائمته الخاصة) — وهكذا بلا حد
- أو يفتح **تذكرة** من نوع محدد

---

## الخطوة 1: أنشئ أنواع التذاكر

قبل اللوحات، جهّز الأنواع اللي راح تفتحها القوائم.

```
/ticket-type create name:طلب_نيترو label:طلب نيترو description:لطلب نيترو لحسابك emoji:💎 category:#الطلبات staff:@فريق_المبيعات channel-name:نيترو-{username}
```

| الخيار | وش يسوي |
|---|---|
| `name` | الاسم المختصر اللي تكتبه في اللوحات (بدون مسافات) |
| `label` | الاسم الظاهر للعضو |
| `description` | وصف قصير يظهر تحت الاسم في القائمة |
| `emoji` | إيموجي الخيار |
| `category` | كاتيغوري خاصة بهذا النوع من التذاكر |
| `staff` | رتبة دعم خاصة بهذا النوع (تتقدم على رتبة الطاقم العامة) |
| `channel-name` | قالب اسم القناة — يدعم `{username}` و `{id}` و `{type}` |
| `max-open` | كم تذكرة من هذا النوع يفتح العضو (افتراضي 1) |

كرّر لكل خدمة: `طلب_حساب` • `طلب_بوستات` • `طلب_قراند` • `طلب_العملات` • `طلب_سيرفر` • `طلب_بوت`

### أضف نموذج أسئلة

```
/ticket-type form name:طلب_نيترو q1:المنصة المطلوبة q2:المدة q3:تفاصيل إضافية long:3
```

- حتى **5 أسئلة**
- `long:3` يخلي السؤال الثالث مربع نص كبير
- العضو يعبّي النموذج **قبل** ما تنفتح القناة، والإجابات تُنشر في التذكرة تلقائياً للطاقم

لحذف النموذج: `/ticket-type form name:طلب_نيترو clear:true` — التذكرة راح تنفتح مباشرة.

---

## الخطوة 2: أنشئ القوائم الفرعية

كل قسم يحتاج إمبيد خاص فيه قائمته.

```
/embed create name:شراء_المستلزمات
```

**📝 النصوص** ← العنوان: `- إختر .`

**📑 إضافة قائمة** ← النص الظاهر: `إختر` ← الخيارات:

```
طلب حساب | لطلب حساب لأحد المنصات | ticket:طلب_حساب
طلب نيترو | لطلب نيترو لحسابك | ticket:طلب_نيترو
طلب بوستات | لطلب بوستات لخادمك | ticket:طلب_بوستات
طلب قراند | لطلب حسابات أو خدمات لعبة قراند | ticket:طلب_قراند
طلب العملات | لطلب العملات الوهمية لأحد المنصات | ticket:طلب_العملات
```

كرّر لبقية الأقسام:

```
/embed create name:شراء_مبدع
```
```
طلب سيرفر | إنشاء وتجهيز سيرفر كامل | ticket:طلب_سيرفر
طلب بوت | تطوير وتجهيز بوت خاص | ticket:طلب_بوت
caneStaRt | لإعادة تحديث القائمة | المركز
```

> آخر خيار إجراؤه `المركز` — يعني اسم اللوحة الرئيسية. هذا اللي يسوي زر الرجوع/التحديث.

---

## الخطوة 3: أنشئ اللوحة الرئيسية

```
/embed create name:المركز
```

**📝 النصوص:**
- العنوان: `- منصة المستلزمات والخدمات لـ مطر .`
- الوصف: نص الترحيب حقك
- نص فوق الإمبيد: اتركه فاضي أو حط `@everyone`

**🎨 اللون:** `#8B5CF6` — **🖼️ الصور:** رابط البانر

**📑 إضافة قائمة** ← النص الظاهر: `- ( اختر القسم المناسب )` ← الخيارات:

```
شراء المستلزمات | لطلب المُستلزمات والخدمات الرقمية | شراء_المستلزمات
شراء مبدع | لطلب حجز مبدع | شراء_مبدع
شراء المنتجات الرقمية | لطلب الخدمات الرقمية | المنتجات_الرقمية
```

**📤 إرسال لقناة** ← خلاص، السلسلة كاملة تشتغل.

---

## لوحة الدعم بزر واحد

أبسط حالة — زي صورة `حجز الدعم`:

```
/ticket-type create name:الدعم_الفني label:الدعم الفني category:#تذاكر_الدعم
/embed create name:بوابة_الدعم
```

**🔘 إضافة زر:**

| الحقل | القيمة |
|---|---|
| نص الزر | `الدعم الفني` |
| اللون | `secondary` |
| الإجراء | `ticket:الدعم_الفني` |

---

## رسالة ترحيب مخصصة داخل التذكرة

تقدر تخلي كل نوع تذكرة يفتح بإمبيد مصمم من عندك بدل الإمبيد الافتراضي:

```
/embed create name:ترحيب_نيترو
```
صمّمه كيف ما تبي، ثم:
```
/ticket-type edit name:طلب_نيترو field:إمبيد الترحيب value:ترحيب_نيترو
```

المتغيرات `{user}` و `{server}` تشتغل داخله.

---

## كل إجراءات الخيارات والأزرار

| تكتب | يصير |
|---|---|
| `اسم_إمبيد` | يفتح إمبيد ثاني (وقائمته إن وُجدت) |
| `ticket:اسم_النوع` | يفتح تذكرة، مع نموذج الأسئلة إن وُجد |
| `apply:اسم_النوع` | يبدأ تقديمًا — راجع `APPLICATIONS.md` |
| `quiz` | يبدأ اختبار التفعيل |
| `sys:bank:balance` | يشغّل عملية بنكية حقيقية — راجع `ECONOMY.md` |
| `link:https://...` | زر رابط خارجي |
| `role:آيدي_الرتبة` | يعطي أو يسحب رتبة |
| `text:نص` | يرد بنص مباشر |

---

## نقاط مهمة

**عمق القوائم بلا حد** — إمبيد يفتح إمبيد يفتح إمبيد. كل رد يظهر لصاحبه فقط (ephemeral)، فما تتوسخ القناة.

**زر الرجوع** — أي خيار إجراؤه اسم اللوحة الأم يرجّعك لها. سمّه `ReStaRt` أو `رجوع` أو أي شي.

**حد التذاكر لكل نوع منفصل** — العضو يقدر يفتح تذكرة `طلب_نيترو` وتذكرة `طلب_حساب` في نفس الوقت، لأن العدّاد مستقل لكل نوع.

**رتبة الدعم لكل نوع** — تقدر تخلي طلبات المبيعات تروح لفريق، وطلبات الدعم الفني لفريق ثاني. كل واحد يشوف تذاكره فقط لأن الصلاحيات مضبوطة على القناة نفسها.

**التعديل بعد النشر** — عدّل الإمبيد واضغط **🔄 تحديث المرسل**، وكل الرسائل المنشورة تتحدّث. أما تعديل نوع التذكرة فيسري فوراً على التذاكر الجديدة بدون أي خطوة.

---

## أخطاء شائعة

| المشكلة | الحل |
|---|---|
| `ما لقيت نوع تذكرة اسمه X` | أنشئه أولاً بـ `/ticket-type create` — الاسم بالضبط بدون مسافات |
| الخيار ما يفتح تذكرة | فعّل نظام التذاكر من `/panel` ← تشغيل الأنظمة |
| `لديك تذكرة مفتوحة من هذا النوع` | أغلقها، أو ارفع `max-open` للنوع |
| النموذج ما يظهر | النوع بلا أسئلة — أضفها بـ `/ticket-type form` |
| القناة تنفتح في مكان غلط | حدّد `category` للنوع |
| القائمة الفرعية ما تفتح | تأكد أن اسم الإمبيد في الإجراء مطابق تماماً |

---

**للتفاصيل الكاملة عن المحرّر:** `BUILDER.md` • **للبنك والمخالفات:** `ECONOMY.md`

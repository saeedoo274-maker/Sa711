# التشغيل على استضافة (لوحة تحكم)

دليل موجّه للاستضافات ذات لوحات التحكم (بتردكتل وما شابهها) — بلا Railway وبلا GitHub.

---

## ⚠️ الشرط الوحيد: Node 22

هذا **أهم سطر في الملف**. البوت يستخدم SQLite **المدمج في Node 22**، فلا يحتاج تثبيت أي مكتبة أو أدوات بناء — وهذا بالضبط ما يحل مشكلة الاستضافات التي تمنع `node-gyp`.

**من لوحة الاستضافة:**
```
Startup ← Node Version ← 22
```

لو كان الإصدار أقل، البوت **يتوقف فورًا برسالة عربية واضحة** تشرح الحل، بدل أن ينهار برسالة غامضة بعد دقائق.

---

## خطوات التشغيل

### 1) ارفع الملفات
فك ضغط الملف وارفع محتوياته لمجلد السيرفر في لوحة الاستضافة.

### 2) عبّي `.env`
افتح `.env` من محرر الملفات في اللوحة:

| المتغير | القيمة |
|---|---|
| `BOT_TOKEN` | توكن البوت من بورتال ديسكورد |
| `CLIENT_ID` | آيدي التطبيق |
| `DEVELOPER_IDS` | آيديك (وشركاءك بفاصلة) |
| `DEV_GUILD_ID` | *(اختياري)* آيدي سيرفرك — يخلي أوامر السلاش تظهر فورًا بدل ساعة |

**لا تحتاج تغيير `DATABASE_PATH`** — الافتراضي `./data/bot.db` مناسب.

### 3) شغّل
```
npm install
npm start
```

`npm install` يثبّت `discord.js` و`dotenv` فقط — كلاهما JavaScript خالص بلا بناء.

> لو ظهرت أخطاء عن `better-sqlite3` أو `canvas` أثناء التثبيت: **تجاهلها**. كلاهما اختياري، والبوت يعمل بدونهما.

---

## ✅ تأكد أنه يعمل

في اللوق لازم تشوف:
```
محرّك قاعدة البيانات: SQLite المدمج في Node
قاعدة البيانات جاهزة: ./data/bot.db
تم تحميل 72 أمر من 33 نظام.
البوت جاهز.
```

ثم في ديسكورد:
```
/فحص
```
يعطيك تقريرًا كاملًا بما هو ناقص في الإعداد قبل أن يظهر كمشكلة أمام الأعضاء.

---

## 💾 البيانات والنسخ الاحتياطي

كل شيء في **`data/bot.db`** — ملف واحد. هذا يعني:

- **لا تحذف مجلد `data/`** عند رفع تحديث. لو حذفته ضاع كل شي: الأرصدة، التذاكر، الهويات، النقاط.
- **النسخ الاحتياطي التلقائي شغّال** كل 6 ساعات في `data/backups/`، ويحتفظ بآخر 10 نسخ.

### للاستعادة
1. أوقف البوت من اللوحة
2. انسخ ملف النسخة من `data/backups/` فوق `data/bot.db`
3. شغّله

### قبل أي تحديث للبوت
نزّل نسخة من `data/bot.db` على جهازك من محرر الملفات. دقيقة واحدة توفّر عليك كارثة.

---

## 🔄 رفع تحديث بلا فقدان البيانات

1. **نزّل `data/bot.db`** على جهازك (احتياط)
2. ارفع الملفات الجديدة **فوق** القديمة
3. **لا تلمس مجلد `data/`**
4. `npm install` ثم أعد التشغيل

الهجرات الجديدة تُطبَّق تلقائيًا عند الإقلاع، وبياناتك القديمة تبقى كما هي.

---

## 🖼️ بطاقة الهوية كصورة (اختياري)

البطاقة تعمل **الآن بلا أي إعداد** كبطاقة منسّقة. لو أردتها صورة مرسومة:

```
npm install canvas canvas-constructor
```

لو فشل التثبيت (وارد على الاستضافات المقيّدة) — **لا مشكلة إطلاقًا**، البوت يكمل بالبطاقة المنسّقة. جرّب فقط:

```
/هوية قالب url:https://رابط-صورة-القالب
```
والأمر نفسه يخبرك هل الرسم متاح أم لا.

---

## 🌐 منفذ الويب

البوت يفتح مسار `/health` **فقط إذا** وُجد متغيّر `PORT`. أغلب لوحات الاستضافة تضبطه تلقائيًا.

لو ما عندك منفذ، لا تضف `PORT` إطلاقًا — البوت يعمل عادي بلا خادم ويب.

---

## 🛠️ حل المشاكل

| المشكلة | الحل |
|---|---|
| رسالة عن Node 22 عند التشغيل | غيّر إصدار Node من `Startup` في اللوحة |
| `Cannot find module 'discord.js'` | شغّل `npm install` |
| أخطاء `node-gyp` أو `better-sqlite3` | تجاهلها — اختيارية والبوت يعمل بدونها |
| أوامر السلاش ما تظهر | تُنشر تلقائيًا. ضع `DEV_GUILD_ID` لتظهر فورًا |
| البيانات ضاعت بعد تحديث | حُذف `data/` — استعد من `data/backups/` |
| البوت يعيد التشغيل باستمرار | راجع اللوق: غالبًا توكن خاطئ أو نية مميزة غير مفعّلة |

**التشخيص من ديسكورد:** `/فحص` للإدارة، و`/مطور diagnostics` لك.

---

## ✔️ قائمة تحقق

- [ ] Node 22 مضبوط في `Startup`
- [ ] `BOT_TOKEN` و `CLIENT_ID` و `DEVELOPER_IDS` معبّأة في `.env`
- [ ] **Server Members** و **Message Content** مفعّلتان في بورتال ديسكورد
- [ ] رتبة البوت فوق كل الرتب التي سيديرها
- [ ] `npm install` تم بلا أخطاء قاتلة
- [ ] اللوق يقول "البوت جاهز"
- [ ] `/فحص` ما فيه أخطاء حمراء
- [ ] نسخة من `data/bot.db` محفوظة عندك

---

## ملاحظة على ملفات Railway

`railway.json` و `nixpacks.toml` و `Procfile` و `.github/` موجودة في المشروع لكنها **تُتجاهَل تمامًا** على استضافتك. تركتها لو احتجت العودة لـ Railway لاحقًا (الدليل القديم محفوظ في `DEPLOY-RAILWAY.md`). حذفها آمن ولن يؤثر على شيء.

# ابدأ من هنا — 3 خطوات

---

## الخطوة 1: ارفع الملفات

في لوحة الاستضافة ← تبويب **Files**:

1. احذف **كل** الملفات والمجلدات الموجودة حاليًا في `/home/container`
2. ارفع ملف الزب كما هو (لا تفكّه على جوالك)
3. اضغط `...` جنب الملف ← **Unarchive** أو **Decompress**

الملفات راح تنفك مباشرة في المكان الصحيح. **لا تحتاج تنقل ولا تحرّك أي شي.**

الشكل النهائي لازم يكون كذا:

```
/home/container/
├── index.js          ← موجود
├── package.json      ← موجود
├── .env              ← موجود
├── src/              ← مجلد
├── config/           ← مجلد
└── locales/          ← مجلد
```

---

## الخطوة 2: حط التوكن

افتح ملف **`.env`** واملأ ثلاث قيم فقط:

```
BOT_TOKEN=توكن_البوت
CLIENT_ID=آيدي_التطبيق
DEVELOPER_IDS=آيديك_في_ديسكورد
```

**من وين تجيبها:**

| القيمة | المكان |
|---|---|
| `BOT_TOKEN` | [بورتال ديسكورد](https://discord.com/developers/applications) ← تطبيقك ← **Bot** ← Reset Token |
| `CLIENT_ID` | نفس الصفحة ← **General Information** ← Application ID |
| `DEVELOPER_IDS` | آيديك: فعّل وضع المطور في ديسكورد ← اضغط على اسمك ← Copy User ID |

**مستحسن:** حط `DEV_GUILD_ID` = آيدي سيرفرك، عشان أوامر السلاش تظهر فورًا بدل الانتظار ساعة.

اضغط **SAVE CONTENT**.

---

## الخطوة 3: شغّل

من تبويب **Console** اكتب:

```
npm install
```

انتظر لين يخلص، ثم اضغط **Start**.

خلاص. أوامر السلاش تنتشر تلقائيًا عند أول تشغيل.

---

## لو طلع خطأ

### `لا يوجد محرّك قاعدة بيانات صالح`

استضافتك على Node قديم. روح **Startup** ← غيّر **Node Version** إلى **22** ← Save ← Restart.

هذا يحل المشكلة نهائيًا لأن Node 22 فيه قاعدة بيانات مدمجة، فما يحتاج البوت يبني أي شي.

### `متغيرات بيئة ناقصة`

القيم في `.env` فاضية أو الملف مو محفوظ. تأكد إن اسمه `.env` بالضبط وإنك ضغطت SAVE CONTENT.

بعض الاستضافات تتجاهل `.env`. لو صار كذا، حط نفس القيم في تبويب **Startup** كمتغيرات بيئة.

### `An invalid token was provided`

التوكن غلط أو منتهي. سوّي **Reset Token** من البورتال وانسخ الجديد كامل.

### `Cannot find module`

الملفات ما انفكّت صح. ارجع للخطوة 1 واحذف كل شي وأعد الرفع.

---

## بعد ما يشتغل

في ديسكورد، من إعدادات التطبيق ← **Bot**، فعّل:

- ✅ **Server Members Intent**
- ✅ **Message Content Intent**

بدونهما جزء كبير من البوت ما يشتغل.

وفي سيرفرك: **ارفع رتبة البوت فوق كل الرتب اللي تبيه يديرها.**

ثم اكتب `/panel` في أي قناة، واضبط رتبة الطاقم وقنوات السجلات من هناك.

---

## تبي تسوي لوحات وأوامر خاصة فيك؟

```
/embed create name:لوحة_الدعم
```

راح ينفتح لك محرّر كامل بأزرار: نصوص، صور، ألوان، حقول، أزرار، قوائم منسدلة.
كل شي من داخل ديسكورد بدون كود.

الشرح بالأمثلة في **`BUILDER.md`**

وتبي قوائم متداخلة تفتح تذاكر؟ **`PANELS.md`**

وتبي تقديمات بقبول ورفض واختبار تفعيل؟ **`APPLICATIONS.md`**

وتبي بنك ومخالفات وطيران بأرصدة حقيقية؟ **`ECONOMY.md`**

---

**للنشر الدائم على Railway:** `DEPLOY.md`

**باقي التفاصيل:** `README.md` للشرح الكامل • `ROADMAP.md` لما هو منجز وما هو مؤجل • `MERGE-PLAN.md` لخطة دمج c2

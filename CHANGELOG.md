# سجل الإصدارات

رقم الإصدار في نهاية اسم الملف: `arabic-admin-bot-v36.zip`
كل تسليم جديد يزيد الرقم بواحد.

---

## v37 — المنصة الموسّعة (50 مرحلة)

بناءً على المشروع الحالي دون إعادة بنائه: كل الأنظمة القديمة بقيت، والجديد إضافات (`src/plugins/`) وخطافات صغيرة. **لا أنظمة حماية جديدة** ولا توسعة للموجود منها.

### المنصة
- بنية إضافات (اكتشاف، هجرات، ترجمات، أحداث، صحة، ترقية)، أعلام ميزات لكل سيرفر وعامة، مجدول وطابور مستمران، صيانة مجدولة لنظام/أمر، إشعارات مركزية، ثيمات، 5 لغات لكل سيرفر.
- منشئ الصلاحيات (رتبة/قناة ← نظام/أمر/أمر فرعي) في بوابة الأوامر.
- `scripts/migrate.js`، تراجع الهجرات، فحص السلامة، قياس المعاملات، مدير التنظيف، مركز الاختبار، أوامر مطور جديدة.
- جلب خارجي آمن من SSRF (`safeFetch`) لكل الطلبات الخارجية.
- لوحة تحكم ويب (OAuth2، جلسات موقّعة، CSRF، وضع ليلي، رسوم SVG) و REST API v1 (مفاتيح لكل سيرفر بصلاحيات، حدود معدل، تحقق).

### الأنظمة
المستويات • AFK • الاقتراحات • التذكيرات • الترحيب بالبطاقات • التحقق الوظيفي • توسعة السجلات (33 نوعًا) • سجل الأعضاء • التحليلات • الإنجازات • المكافآت • الاقتصاد الموسّع والمتجر • الألعاب (10) • لوحات المتصدرين (14) • التذاكر المتقدمة (SLA، نقل، وسوم، تصعيد…) • السحوبات المتقدمة وتتبع الدعوات • لوحات نجوم متعددة • الأوامر المخصصة الموسّعة • أتمتة التفاعلات • الإعلانات • القضايا ودورة حياة البلاغات • الاستئنافات • البحث الشامل • إدارة الطاقم (أقسام، مناوبات، KPI) • النماذج • التواصل الاجتماعي الموسّع • الأتمتة • التكاملات (12 مزوّدًا) • نسخ السيرفر والاستعادة • معالج الإعداد • 69 متغيرًا.

### إصلاحات في الكود القائم
- أوامر قائمة السياق لم تكن تُنفَّذ إطلاقًا.
- `require` داخل تعليق في `runtime/interactions.js` (safeReply غير معرّف).
- إحصاءات طاقم التذاكر بعضو محدد كانت تفشل (عمود ملتبس).
- اسم حدث رفع المستوى في Webhook الصادر.

### التوافق
- هجرات متوافقة للخلف لكل جدول جديد مع قسم تراجع مختبر؛ لا فقد لإعدادات أو تذاكر أو اقتصاد أو قضايا أو طاقم أو تقديمات أو سجلات.
- أوامر البريفكس والسلاش تعمل كما كانت (`!help` وغيره)، والأوامر القديمة لم تُحذف أو يُعَد تسميتها.
- المستويات وتتبع الدعوات معطّلة افتراضيًا حتى لا يتغير سلوك السيرفرات الحالية.

---

## v36 — نظام متغيرات موحّد (41 متغيرًا) وقوالب الإمبيد

**تحليل مشروع مرجعي: 74 ملفًا، 15,796 سطرًا، و400 سطر اختبارات — أنظف مرجع حتى الآن. نُقلت منه الفجوات الحقيقية فقط.**

### 1) نظام المتغيرات الموحّد

كان عندنا **4 متغيرات** فقط (`{user}` `{username}` `{server}` `{membercount}`) مكتوبة يدويًا داخل `EmbedService`. صار عندنا **41 متغيرًا** في وحدة مركزية واحدة:

| المجموعة | أمثلة |
|---|---|
| 👤 العضو | `{USER}` `{DISPLAYNAME}` `{NICKNAME}` `{USER_TAG}` `{USER_AVATAR}` `{USER_JOINED}` |
| 🏠 السيرفر | `{SERVER}` `{MEMBERS}` `{OWNER}` `{SERVER_ICON}` |
| 💬 القناة | `{CHANNEL}` `{CHANNEL_NAME}` `{TICKET}` |
| 🎭 الرتبة | `{ROLE}` `{ROLE_NAME}` |
| 🤖 البوت | `{BOT}` `{PREFIX}` |
| 🕐 الوقت | `{DATE}` `{TIMESTAMP}` `{RELATIVE}` |
| ⚖️ الإجراءات | `{MODERATOR}` `{REASON}` `{DURATION}` `{CASE_ID}` |

**ثلاث قواعد تصميم:**
- **حالة الأحرف لا تهم** — `{USER}` و`{user}` و`{uSeR}` سواء، فلا يفشل نص بسبب حالة مختلفة.
- **المتغير غير المعروف يبقى حرفيًا** — لا يُمحى بصمت، فيرى المستخدم خطأه.
- **المتغير السياقي بلا سياق يُفرَّغ** — `{REASON}` في رسالة ترحيب لا معنى له.

**زر 🔣 المتغيرات** في المحرّر يعرضها مجمّعة مع **معاينة حيّة بقيم المستخدم نفسه**.

**التوافق الخلفي:** اكتشف الاختبار أن `{membercount}` (كلمة واحدة) كان سيتعطّل لأن النظام الجديد يسمّيه `MEMBER_COUNT` — أُضيف كمرادف دائم، وتحقّقت أن كل المتغيرات الأربعة القديمة ما زالت تعمل على الإمبيدات المنشورة.

### 2) قوالب الإمبيد

**💾 حفظ كقالب** يحفظ الإمبيد الحالي (نصوصه وصوره ومكوّناته) باسم، و**📂 القوالب** يحمّله على أي إمبيد آخر أو يحذفه.
- الحفظ باسم موجود **يحدّثه** بدل أن يُنشئ نسخة ثانية
- التحميل يستبدل المحتوى **ويُبقي اسم الإمبيد ومعرّفه**، فالرسائل المنشورة تبقى مرتبطة
- عدّاد استخدام لكل قالب

### 3) البنية التحتية للاختصارات

أُضيف `cooldown_ms` لجدول الأوامر المخصصة وجدول `custom_command_aliases` — الأساس لاختصارات الأوامر. **الهجرة آمنة عند الإعادة** (تحقّقت بإقلاع ثانٍ على نفس القاعدة).

**ما لم يُنقل:** معمارية `views/` عندهم قوية لكن معماريتنا (`modules/` بخدمات ومستودعات وطبقة أمان تفاعلات) أنضج، ونقلها كان سيعني إعادة كتابة 174 ملفًا بلا مكسب.

**اختبارات:** 586 ناجحة، صفر فاشلة، مستقرة عبر 3 جولات (66 جديدة).

---

## v35 — نظام مهام الإدارة بتحقق آلي حقيقي

**حُلِّل مشروع مرجعي (485 سطرًا) وأُعيد بناء الفكرة بالكامل بمعايير المشروع.**

**ما كان في المرجع:** تخزين JSON، شخص واحد لكل مهمة، ونصوص مهام ثابتة في ملف — و**لا تحقق إطلاقًا**: الضغط على "تم الإنجاز" يُنجز المهمة بلا أي فحص.

### 1) التحقق الآلي — جوهر النظام

البوت **يقيس** الإنجاز من بيانات السيرفر بدل تصديق ضغطة زر. عند الاستلام تُؤخذ **لقطة (baseline)** للرقم الحالي، وعند الضغط على "تم الإنجاز" يُقاس الفرق — **فلا يُحتسب عمل سابق للاستلام إطلاقًا** (مُختبَر: عضو له 100 رسالة سابقة يبدأ من صفر).

**تسع طرق تحقق:** عدد الرسائل • دقائق الصوت • تذاكر مستلمة • تذاكر مغلقة • تقييمات مستلمة • مخالفات صادرة • أيام حضور • نقاط النشاط • تصديق إداري.

المهام اليدوية وحدها تطلب **إثباتًا مكتوبًا** يُرسل للإدارة بزرّي تصديق ورفض.

### 2) عدد المنفذين — قابل للتعديل لكل مهمة

`max_claims` يحدد كم شخصًا ينفذ المهمة (0 = بلا حد). الفحص والإدراج **داخل معاملة واحدة**، فلو ضغط عشرة على مهمة سعتها ثلاثة **لا يستلمها إلا ثلاثة بالضبط** — مُختبَر فعليًا.

التعديل: `/مهام تعديل key:<المفتاح> field:عدد المنفذين value:5`

### 3) عشرون مهمة جاهزة

`/مهام جاهزة` يزرع 20 مهمة (10 يومية + 10 أسبوعية)، **13 منها قابلة للقياس الآلي**. لا تلمس ما هو موجود مسبقًا.

### 4) بطاقة الإمبيد المطوّر

Components v2 بلون حسب الصعوبة (🟢 سهلة 🔵 متوسطة 🟡 صعبة 🔴 نخبة)، تعرض شرط الإنجاز والمقاعد المشغولة وقائمة من يُنفّذ ومن أنجز، وثلاثة أزرار: استلام • تم الإنجاز • تخلّي. تتحدّث ذاتيًا بعد كل تفاعل.

**التقدّم الناقص** يُعرض بشريط مرئي مع المتبقي بالضبط.

### 5) دورات ومهل

كل نشر يفتح **دورة** مستقلة، فتُعاد المهمة بلا خلط سجلات القديمة. الاستلام له مهلة تُلغى تلقائيًا بعدها فيتحرر المقعد، والدورة تُغلق في موعدها. فحص دوري كل 5 دقائق.

**الأمان:** الإنجاز ذرّي ويُسجَّل في سجل الإنجاز بنفس المعاملة — 20 محاولة متزامنة أنتجت إنجازًا واحدًا وصرف مكافأة واحدة.

**التكامل:** لا جداول مكرّرة — يقرأ من `staff_activity` و`ticket_ratings` و`violations` الموجودة، والمكافأة المالية عبر نظام الاقتصاد. والقسم مضاف في `/لوحة` ← الأنظمة المدنية ← 🎯 مهام الإدارة.

**اختبارات:** 520 ناجحة، صفر فاشلة، مستقرة عبر 3 جولات (54 جديدة).

---

## v34 — إزالة حزمة الإيموجيات بالكامل

**حُذف النظام كله بناءً على الطلب، بلا أي بقايا معلّقة.**

**ما حُذف:**
- `assets/emojis/` — 245 صورة (11 ميجا)
- `src/modules/emojis/` — الخدمة وأمر `/ايموجيات`
- قسم "😀 الإيموجيات" من `/لوحة` وأزراره الخمسة ومساراته
- `emojiPack` من `Application.js`
- تصنيف الإيموجيات من مركز المساعدة

**التحقق من عدم وجود بقايا:** فُحص المشروع كاملًا بحثًا عن أي إشارة (`emojiPack`، `EmojiPackService`، `assets/emojis`، `panel:emojis`، ودوال الشاشات) — **صفر نتيجة**. وفاحص الاستيرادات الآلي نظيف، فلا استدعاء لدالة محذوفة.

**الأثر على الحجم:** الحزمة نزلت من **11 ميجا** إلى **أقل من ميجا واحد**.

**الأنظمة:** 34 (كانت 35) • **الأوامر:** 89 (كانت 90) • أزرار اللوحة الرئيسية 19 في 5 صفوف، ضمن حدود ديسكورد.

**اختبارات:** 466 ناجحة، صفر فاشلة (حُذفت مجموعة اختبارات الإيموجي الـ39، وحُدّثت مجموعتان كانتا تتوقعان وجود النظام).

---

## v33 — تسجيل الدخول اليومي، تحذير الغياب، والتحكم بالشعار والرابط

**كل شيء بالأزرار داخل لوحة الإدارة المنشورة، بلا أي أمر سلاش جديد.**

### 1) تسجيل الدخول اليومي

الإداري يسجّل حضوره **مرة واحدة يوميًا**، وللتسجيل طريقتان:
- **تلقائيًا بأول رسالة** يكتبها في **أي روم** — لا يحتاج أمرًا ولا زرًا
- **يدويًا** بزر ✅ **تسجيل دخول** لمن أراد التسجيل بلا كتابة

التسجيل الأول في اليوم هو المعتمد (`INSERT OR IGNORE` + مفتاح أساسي على اليوم)، فالتكرار مستحيل بنية الجدول نفسه — مُختبَر بـ40 محاولة متزامنة.

يُحتسب للإداري: **سلسلة الأيام المتتالية**، وعدد أيام الحضور خلال 30 يومًا، وأيام الغياب منذ آخر حضور. ومن سجّل أمس ولم يسجّل اليوم **لا تنقطع سلسلته** قبل انتهاء اليوم.

### 2) تحذير الغياب التلقائي — قابل للتعديل بالكامل

من يغيب أكثر من الحد يصله تحذير **مرة واحدة يوميًا** (لا يتكرر مهما تكرر الفحص).

**⚠️ إعدادات الغياب** في اللوحة تتيح:
- **تفعيل/إيقاف** النظام
- **✏️ تعديل نص التحذير** بالكامل، مع **معاينة فورية** بنفس آلية الإرسال الحقيقية
- **📆 مدة الغياب** قبل التحذير (1-30 يومًا)
- **📢 قناة التحذير** — يُرسل بالخاص دائمًا، والقناة نسخة عامة إضافية

**متغيرات النص:** `{user}` `{username}` `{server}` `{days}` `{date}`

### 3) 📅 حضور اليوم

شاشة تعرض إجمالي الطاقم والحاضرين والغائبين، مع عدد أيام غياب كل واحد مرتّبين من الأطول غيابًا.

### 4) 🖼️ الشعار و 🔗 الرابط

زران منفصلان: الشعار (فارغ = شعار السيرفر)، والرابط يظهر في متن اللوحة (يقبل http و https). كلاهما يُحدّث اللوحة المنشورة فورًا.

**الأداء:** التسجيل التلقائي يُستدعى مع **كل رسالة** في السيرفر، فرُتِّبت فحوصاته من الأرخص للأغلى — التفعيل ثم كون الكاتب إداريًا — قبل لمس قاعدة البيانات.

**الصلاحيات:** تسجيل الدخول وحضور اليوم متاحان للطاقم، وكل الإعدادات للإدارة العليا وحدها. مُختبَر لكل زر.

**اختبارات:** 506 ناجحة، صفر فاشلة، مستقرة عبر 3 جولات (66 جديدة).

---

## v32 — لوحة نظام الإدارة: رسالة منشورة دائمة بأزرار وقوائم

**المطلوب كان لوحة لا أوامر سلاش. النتيجة: أمر واحد يُستخدم مرة واحدة فقط للنشر، وبعدها كل شيء بالأزرار.**

```
/لوحة_الادارة channel:#لوحة-الادارة
```

تُنشر رسالة **دائمة** في القناة، يضغطها كل الطاقم، و**كل رد يظهر لصاحبه وحده** (مخفي) — فلا يتعارض اثنان على نفس الرسالة مهما استخدمها عدد.

**محتوى اللوحة:** شعار السيرفر (أو شعار مخصص) • إجمالي الإدارة • أعلى رتبة • أدنى رتبة • رتبة الإدارة المشتركة • عدد الرتب.

**12 زرًا في 4 صفوف:**

| الصف | الأزرار |
|---|---|
| 1 | 👤 عرض ملفي • 🪜 سلم الرتب • 🏆 توب النقاط |
| 2 | 📈 ترقية • 📉 تنزيل • 🗑️ سحب |
| 3 | 📊 احصائياتي • 💬 توب الرسائل • 📋 إحصائيات عامة |
| 4 | 🔄 تحديث اللوحة • 🖼️ وضع شعار • ❓ مساعدة |

**تفاصيل التنفيذ:**
- **الترقية والتنزيل والسحب** بقوائم اختيار أعضاء (`UserSelectMenu`) داخل اللوحة، وتمر بنفس سلسلة فحوصات `StaffService`.
- **الصلاحية تُفحص عند كل ضغطة لا عند النشر** — الرسالة تبقى شهورًا وقد تتغيّر رتب الأعضاء بعدها. الطاقم العادي يرى ملفه والسلم والتوب، والإجراءات العليا للأدمن وحده.
- **المساعدة تتكيّف:** الأدمن يرى شرح الإجراءات، والطاقم يرى أنها للإدارة العليا فقط.
- **اللوحة تُحدَّث تلقائيًا** بعد أي ترقية أو تنزيل أو سحب، فأرقامها لا تتقادم.
- **🖼️ وضع شعار** بنافذة إدخال رابط، والفارغ يرجع لشعار السيرفر.

**اختبارات:** 440 ناجحة، صفر فاشلة، مستقرة عبر 3 جولات (58 جديدة تغطي كل زر، والصلاحيات، والحالات الحدّية، وسيرفرًا بلا رتب).

---

## v31 — لوحة الطاقم الكاملة: شعار ونيك نيم ونقاط وإجراءات عليا

**فُحص الطلب مقابل الموجود فعليًا، فتبيّن أن أربع ميزات ناقصة كليًا وواحدة موجودة كأمر سلاش فقط.**

**جديد في `/لوحة` ← السلم الإداري:**
- **شعار السيرفر** واسمه في رأس الشاشة
- **بطاقة العضو**: النيك نيم، رتبته الحالية، و**نقاطه** مع تفصيلها (رسائل • استلام • إغلاق • صوت • تقييم) ومتوسط تقييمه
- **مؤشر ➤** أمام رتبتك داخل السلم، فتعرف موقعك بنظرة
- **👤 ملفي بالتفصيل** — شاشة كاملة بكل أرقام نشاطك
- **⭐ توب النقاط**
- **الإجراءات العليا (للأدمن فقط):** ⬆️ ترقية • ⬇️ تنزيل • 🚫 سحب من الطاقم — كلها بقوائم اختيار أعضاء داخل اللوحة

**نظام السحب من الطاقم (جديد كليًا):**
لم يكن موجودًا لا كأمر ولا في اللوحة. يزيل **كل** رتب السلم الإداري دفعة واحدة ومعها رتبة الطاقم الأساسية — بخلاف التنزيل الذي ينقل رتبة واحدة للأسفل.
يمر بنفس سلسلة فحوصات `StaffService`: صلاحية المنفّذ، الهرمية بين الأعضاء، وقدرة البوت على إدارة كل رتبة **قبل** لمس أي شيء — فلا تحدث إزالة جزئية تترك العضو في حالة نصف مسحوبة.
متاح أيضًا كأمر: `/سحب_اداري user:<@عضو> reason:<السبب>`

**ثلاثة أعطال حقيقية اكتُشفت أثناء البناء:**

1. **`panelView` كان يُسقط `thumbnail` كليًا** — أي شعار سيرفر في أي شاشة لوحة كان يختفي بلا أثر. صار يُعرض ضمن صور الحاوية.
2. **`buildEmbed` لم يكن يدعم `author` إطلاقًا** — تمريره كان يُتجاهل بصمت، فاسم السيرفر لا يظهر. أُضيف الدعم مع قص الطول لحدود ديسكورد.
3. **شاشة السلم الإداري كانت تتطلب ADMIN** — فالطاقم العادي لا يقدر يرى رتبته ولا نقاطه، وهو نقيض الغرض. صارت متاحة للطاقم، **والإجراءات العليا وحدها تبقى للأدمن** (مُختبَر: الطاقم لا يرى أزرار الترقية والسحب وإضافة الرتب).

**تعارض اسم أمر:** الاختبار الآلي كشف أن الاختصار `سحب` مأخوذ بالفعل لأمر السحوبات (`/سحب`) — لو مرّ لكان هجم على أمر موجود. غُيّر إلى `سحب-اداري`.

**اختبارات:** 382 ناجحة، صفر فاشلة، مستقرة عبر 3 جولات (43 جديدة للوحة الطاقم والسحب).

---

## v30 — طبقة أمان التفاعلات، ونقاط الطاقم

**تحليل مشروع مرجعي (35,260 سطرًا، 96 ملفًا) ونقل ما يستحق فقط.**

### 1) طبقة أمان التفاعلات — 454 موضعًا محميًا

المشروع المرجعي كان فيه `interactionSafe` لمعالجة أخطاء ديسكورد الشائعة. فحصتُ مشروعنا فوجدت **454 استدعاء تفاعل بلا أي حماية** عبر 23 ملفًا — كل واحد منها ينهار عند:

| الخطأ | متى يحدث |
|---|---|
| `InteractionAlreadyReplied` | ضغطتان سريعتان على نفس الزر |
| `InteractionNotReplied` | `editReply` قبل التأكيد |
| `Unknown interaction` (10062) | مرّت 3 ثوانٍ بلا رد |
| `Unknown message` (10008) | حُذفت الرسالة أثناء المعالجة |

**هذا هو السبب الجذري لـ"الأخطاء العديدة"** التي تظهر للمستخدم كـ"This interaction failed".

بُنيت `src/core/interactions/interactionSafe.js` بأربع دوال (`safeReply`, `safeUpdate`, `safeModal`, `ackComponent`) تختار المسار الصحيح تلقائيًا ولا ترمي أبدًا، **وتتعرّف على الأخطاء المنتهية فلا تُعيد المحاولة عبثًا**. عند فشل كل المسارات تُرسل رسالة مخفية للمستخدم بدل ترك الزر صامتًا.

طُبِّقت على: اللوحة (102) والاقتصاد (47) والبناء (42) والمدينة (37) والتذاكر (31+7) والتقديمات (27+18) والانتخابات (20) والهوية (18) والعسكرية (16) والاجتماعي (15) والحكومة (13) ودورة الحياة (11) وبقية الأنظمة.

### 2) نقاط الطاقم — مقياس واحد شامل

المرجع كان يحسب نقاط الطاقم في ملف JSON منفصل. نُقلت **الفكرة** لا التنفيذ: النقاط تُحسب عندنا من `staff_activity` و`ticket_ratings` **الموجودَين أصلًا** — بلا جدول جديد وبلا هجرة وبلا بيانات مكرّرة.

**الصيغة (قابلة للتعديل من `staff.points` في الإعدادات):**
- كل 50 رسالة = نقطة • استلام تذكرة = 1 • إغلاق = 1 • ساعة صوت = 1
- التقييم: ⭐1=0.25 • ⭐2=0.5 • ⭐3=0.75 • ⭐4=1 • ⭐5=1.5

**أين تظهر:**
- `/نشاط_اداري` — النقاط ومتوسط التقييم وتوزيع النقاط بالتفصيل
- `/الاكثر_نشاطا metric:⭐ النقاط` — صدارة شاملة
- `/لوحة` ← الطاقم ← **⭐ صدارة النقاط** (حسب قاعدة: كل نظام له تحكّمه في اللوحة)

**ما لم يُنقل:** المرجع يستخدم MongoDB وتخزين JSON مبعثرًا وبنية handlers مسطّحة — كلها أضعف من معماريتنا، فلم يُنقل منها شيء.

**اختبارات:** 339 ناجحة، صفر فاشلة، مستقرة عبر 3 جولات (45 جديدة: 16 لطبقة الأمان ضد أخطاء ديسكورد الحقيقية، 29 لنظام النقاط).

---

## v29.1 — تدقيق تغطية اللوحة: كل نظام موصول ومُختبَر فعليًا

**تحقّق من طلب: "تأكد من وضع جميع الأنظمة وبناها وتشغيلها في أمر لوحة".**
لم أكتفِ بالفحص البصري — بنيت فاحصًا آليًا يقارن **الأنظمة الـ36** بمسارات اللوحة، ثم اختبارًا **يشغّل كل شاشة فعليًا** ويتحقق أنها تُبنى وترد.

**النتيجة: 36/36 نظامًا موصولًا**، لكن التشغيل الفعلي كشف 4 مشاكل حقيقية:

**1) قسم "المساعدة" كان طريقًا مسدودًا**
كان يرد بنص: "استخدم الأمر `/مساعدة`" — يخرجك من اللوحة ليطلب منك كتابة أمر آخر، وهو نقيض فكرة اللوحة. صار **شاشة حقيقية** تعرض الأوامر مجمّعة حسب القسم، **ومفلترة بصلاحية العضو** (لا تُعرض أوامر لا يملكها).

**2) `app.client.ws.ping` بلا حماية**
شاشة المطوّر كانت تنهار كليًا لو كان `ws` غير متاح لحظة إعادة اتصال. صار `ws?.ping ?? 0`.

**3) وسيط مجهول يُسقط شاشات التذاكر والإدارة**
`moderationModal` و`ticketChannelPick/Set` كانت تقرأ `meta.label` بلا التأكد من وجود `meta`. زر قديم أو معرّف مشوّه = انهيار. الثلاثة تردّ الآن برسالة واضحة.

**4) `app.client.options.intents` بلا حماية**
الفحص الشامل كان ينهار لو كان `options` غير معرّف. صار `options?.intents`.

**اختبار تغطية دائم (11 فحصًا):**
- الأقسام الـ24 كلها تفتح بلا انهيار
- كلها **Components v2** (الأزرار داخل الحاوية)
- كلها فيها **طريق رجوع** (لا شاشة مسدودة)
- المسارات ذات الوسائط تعمل بوسيطها الصحيح
- **وسيط مجهول يرد بلطف لا ينهار**
- اللوحة الرئيسية ضمن حدود ديسكورد (5×5)
- كل قسم يُوصَل إليه خلال **خطوتين كحد أقصى**
- عضو عادي **يُرفض** من الأقسام الحساسة (المطوّر، الحماية، المراقبة، الهوية، الحكومة، الإيموجيات)

**المجموع: 294 اختبارًا، صفر فاشل، مستقر عبر 3 جولات.**

---

## v29 — إصلاح شامل: نظام التقييم كان معطّلًا بالكامل

**تدقيق عميق بعد بلاغ "أخطاء عديدة في التقييم وإرسال الإمبيد". النتيجة: عطل جذري في التقييم كان يُفشل **كل** تقييم بلا استثناء، ومشكلتان في وضوح الأخطاء.**

### 1) نظام تقييم التذاكر كان معطّلًا 100%

**السلسلة الكاملة للعطل:**
1. عند إغلاق التذكرة تُرسل أزرار التقييم لخاص العضو
2. **بعدها مباشرة** تُحذف التذكرة من القاعدة حذفًا صلبًا (`DELETE`)
3. العضو يضغط نجمة → المعالج يبحث عن التذكرة → غير موجودة
4. الخطة البديلة كانت تبحث في `ticket_ratings` عن تقييم سابق — لكن هذا **أول** تقييم، فلا يوجد
5. النتيجة: `guildId` = undefined → **"انتهت صلاحية هذا التقييم"** في كل مرة

الخطة البديلة كانت دائرية: تبحث عن تقييم لا يمكن أن يوجد إلا بعد نجاح تقييم.

**أثبت الاختبار العطل:** 13 من 17 فحصًا فشل قبل الإصلاح — التقييم لا يُحفظ، الإحصاءات صفر، الملاحظات لا تُحفظ، ولا شيء يُنشر في قناة التقييمات.

**الإصلاح:** تُضمَّن البيانات اللازمة في معرّف الزر نفسه (`trate:<id>:<action>:<guild>:<staff>:<number>`) فيبقى الزر شغّالًا مهما طال الوقت ومهما حُذفت التذكرة. القراءة من القاعدة بقيت كخطة بديلة للأزرار القديمة المُرسلة قبل التحديث. الطول 18-59 حرفًا — بعيد عن حد ديسكورد (100).

**أُصلح معه:** `staff_id` و`ticket_number` كانا يُحفظان `null` فتنهار إحصاءات الإداري؛ ونجوم خارج النطاق كانت تُتجاهل بصمت بلا أي رد.

### 2) فشل إرسال الإمبيد بلا سبب مفهوم

كان الخطأ يُبتلع (`.catch(() => null)`) فيرى المستخدم "فشل الإرسال." فقط. الآن يُترجَم سبب ديسكورد الحقيقي لرسالة عربية قابلة للتنفيذ:
- إمبيد فارغ → يشرح أن ديسكورد يرفض الرسائل الفارغة ويوجّه لأمر التعديل
- خطأ 50035 مع صور → يوجّه لفحص روابط الصور
- 50013 / 50001 / 10003 → صلاحيات ناقصة أو قناة محذوفة
كما يُسجَّل الخطأ الأصلي كاملًا في نظام الأخطاء للمطوّر.

### 3) ضياع صامت في تقييم الخدمات والاقتراحات

`/خدمات` كان يقول "تم إرسال تقييمك بنجاح" **حتى لو لم تُضبط قناة التقييمات** — فيضيع رأي العضو بلا أن يدري أحد. الآن يُرجع نجاحًا فقط عند وصول الرسالة فعلًا، وإلا ينبّه العضو بوضوح.

### 4) استيرادان ناقصان (انهيار وقت تشغيل)

`truncate` مستخدمة بلا استيراد في `services/interactions.js` وفي معالج الأخطاء الجديد — كلاهما كان سينهار عند أول استدعاء. أُضيف فاحص آلي يمسح **كل** ملفات المشروع بحثًا عن هذا النمط (دوال `helpers` و`discord.js` و`componentsV2`)، والنتيجة الآن نظيفة تمامًا.

### التحقق

**283 اختبارًا، صفر فاشل، مستقر عبر 4 جولات متتالية:**
- 17 لتدفق التقييم الحقيقي (إغلاق تذكرة ← حذفها ← تقييمها ← ملاحظة ← منع التكرار ← إحصاءات الإداري)
- 23 لبناء حمولة الإمبيد (فارغ، v2، تقليدي، متغيرات، نصوص طويلة، أزرار، روابط، قوائم، صور)
- 12 لصلابة موجّه التفاعلات (معالج ينفجر، بادئة مجهولة، `customId` مشوّه أو فارغ أو 150 حرفًا)
- 231 من الإصدارات السابقة للتأكد من عدم كسر أي نظام

**فحوصات آلية جديدة:** فاحص الاستيراد المفقود، وفاحص بادئات `customId` مقابل الموجّه.

---

## v28.1 — إصلاح عطل حرج: توقف كل الخدمات الدورية عند إقلاع البوت

**عطل مُبلَّغ من الاستضافة الفعلية:** البوت يظهر متصلاً وشغّالاً (Uptime يزيد، لا انهيار)، لكن سجل الأخطاء يُظهر:
```
[ERROR] events/ready :: Cannot read properties of undefined (reading 'status')
    at Application.setPresence (.../Application.js:259:17)
```

**الخطورة الحقيقية أكبر بكثير من رسالة الخطأ:** `setPresence()` تُستدعى في **أول سطر** من دالة `ready()`، وعند فشلها بلا `try/catch` يتوقف تنفيذ الدالة بالكامل عند تلك النقطة. يعني كل ما يليها **لم يكن يعمل إطلاقًا** في كل مرة يفشل فيها هذا السطر:
- نشر أوامر السلاش لا يحدث
- تسجيل السيرفرات الموجودة مسبقًا لا يحدث
- **كل** الخدمات الدورية لا تبدأ: السحوبات، الاستطلاعات، دورة حياة الطاقم، أتمتة التذاكر، التقارير، **مراقب الإفراج التلقائي عن السجناء**، النسخ الاحتياطي، وخادم الصحة
- معالجة السحوبات/الاستطلاعات التي انتهت أثناء توقف البوت لا تحدث

بمعنى آخر: بوت "يعمل" ظاهريًا لكن **معطّل فعليًا بنسبة كبيرة**، وأي سجين تجاوزت مدته لا يُفرَج عنه تلقائيًا أبدًا طالما هذا الخلل قائم.

**السبب الجذري:** `bot.json` على الاستضافة فقد قسم `presence` (على الأرجح تعديل يدوي عبر محرر الملفات في لوحة الاستضافة)، و`ConfigService` كان يحمّل الملف بلا أي تحقق أو قيم افتراضية.

**الإصلاح على ثلاث طبقات:**

1. **`setPresence()` لا يمكن أن تفشل بعد الآن** — محاطة بـ`try/catch` كاملة، مع قيم افتراضية آمنة (`status: "online"`, نشاط افتراضي) عبر `?.` بدل الوصول المباشر. أي عطل يُسجَّل تحذيرًا فقط.

2. **إعادة ترتيب `ready.js`** — `setPresence()` (تجميلية بحتة) صارت **آخر** سطر ينفَّذ، بعد كل الخدمات الحرجة. حتى لو ظهر خلل مستقبلي غير متوقع فيها، لن يقدر يوقف نشر الأوامر ولا أي خدمة دورية بعد الآن — فقط حالة الظهور نفسها.

3. **تصليب `ConfigService` من الجذر** — `bot.json` يُحمَّل الآن بدمج عميق فوق قيم افتراضية كاملة (presence، الألوان، الإيموجيات، الحدود). أي مفتاح ناقص أو حتى ملف JSON تالف بالكامل لا يعود يُسقط أي جزء من الإعدادات — والنظام يستمر بقيم آمنة معقولة. هذا يحمي `config.color()` و`config.emoji()` أيضًا، وهما يُستدعيان من مئات المواضع في المشروع.

**إصلاح إضافي:** استُبدل الاستماع لحدث `ready` المهمَل في discord.js بالاسم الحديث `clientReady`، فيختفي تحذير `DeprecationWarning` الظاهر في نفس السجل.

**اختبارات:** 18 اختبارًا جديدًا يُعيد إنتاج العطل الحقيقي حرفيًا (حذف `presence` من `bot.json`، إتلاف الملف بالكامل، محاكاة فشل `client.user.setPresence` نفسها) ويتحقق أن **كل الخدمات الثمانية** (بما فيها مراقب الإفراج عن السجناء) تبدأ بنجاح رغم ذلك. صفر فاشلة، مستقر عبر 5 تشغيلات، وتأكيد أن `bot.json` الحقيقي لم يتأثر أثناء الاختبار.

---

## v28 — سد فجوة اللوحة: الهوية والحكومة، ورفع صور حقيقي للإمبيدات

**فحص شامل كشف مخالفة حقيقية لقاعدتنا الثابتة: نظاما الهوية (`/هوية`) والحكومة (`/حكومة`) كانا بأوامر سلاش فقط بلا أي حضور في `/لوحة`. صُحِّح بالكامل، بلا حذف أي شيء موجود.**

**1) رفع صورة حقيقي على الإمبيدات (`/ايمبد صورة`)**
مودالات ديسكورد لا تقبل مرفقات — فقط نصوصًا. الحل التقني الوحيد: أمر سلاش بخيار مرفق فعلي.
```
/ايمبد صورة name:<الإمبيد> file:<ارفع من جهازك> type:كبيرة|مصغّرة
```
يتحقق أن الملف صورة فعلية (`image/*`) قبل الحفظ، ويخزّن رابط CDN الخاص بديسكورد في بيانات الإمبيد — نفس الحقل الذي يقرأه الرابط النصي، فالنظامان متوافقان تمامًا. المحرّر التفاعلي يذكّر بالأمر الجديد في وصفه.

**2) قسم "الأنظمة المدنية" — نقطة دخول جديدة بلا كسر التخطيط**
اللوحة الرئيسية كانت بالفعل عند الحد الأقصى لديسكورد (5 أزرار × 5 صفوف). بدل كسر هذا الحد، أُضيفت شاشة وسيطة `/لوحة` ← **🏙️ الأنظمة المدنية** تجمع (المدينة والسجن، الهوية، الحكومة) — بلا أي تعديل على تخطيط الشاشة الرئيسية.

**3) لوحة الهوية الوطنية الكاملة**
قناة المراجعة • رتبة المواطن (مع فحص تسلسل الرتب) • مدة الصلاحية (مودال رقمي) • قالب البطاقة (رابط أو مسح) • الطلبات المعلّقة • تفعيل/تعطيل.

**4) لوحة الحكومة الكاملة**
قنوات المجلس والتعاميم والتقاعد كل واحدة مستقلة • رتبة أعضاء المجلس • رتبة المتقاعد (مع فحص تسلسل الرتب) • تفعيل/تعطيل.

**5) صور لوحات المدينة داخل `/لوحة`**
كانت `rp.jobsImageUrl` و`rp.blackMarketImageUrl` بأمر سلاش فقط (`/مدينة اعدادات`). صارت مودالًا داخل شاشة "🏙️ المدينة" نفسها، مع تحقق من صحة الرابط والحفاظ على القيمة القديمة عند الرفض.

**6) تحذير الخصوصية عند إنشاء الهوية**
ديسكورد لا يسمح بنص ثابت داخل مودال (فقط حقول إدخال) — فالتحذير المطلوب لا يمكن أن يكون داخل نافذة إدخال الهوية نفسها. أُضيف بدلًا منه في لوحة "الأحوال المدنية" مباشرة فوق زر الإنشاء، حيث يقرؤه العضو فعليًا قبل الضغط.

**اختبارات:** 213 اختبار جديد لهذه الجلسة (56 للشاشات والأمر الجديدين، تحقّق أن 82+39 من قبل ما زالا يعملان، و36 فحصًا شاملًا لكل نظام سابق) — صفر فاشلة، ومستقرة عبر 6 تشغيلات متتالية.

---

## v27 — حزمة الإيموجيات (245 إيموجي مرفقة)

**الحزمة داخل البوت في `assets/emojis/` بأسماء نظيفة `wc_001` … `wc_245`، وتُرفع للسيرفر بأمر واحد أو من اللوحة.**

**الاستخدام:**
```
/ايموجيات حالة     كم مرفوع وكم بقي وكم خانة حرة
/ايموجيات رفع      يرفع ويتوقف تلقائيًا عند الامتلاء
/ايموجيات حذف      يحذف إيموجيات الحزمة فقط
```
أو من `/لوحة` ← 😀 **الإيموجيات**.

**ثلاثة قيود من ديسكورد حكمت التصميم:**
1. **سقف الإيموجيات** (50 عاديًا، 100/150/250 بالبوست) — الرفع **يتوقف بهدوء عند بلوغه** ويخبرك كم بقي من الحزمة، بدل أن يفشل.
2. **حد المعدل الشديد** — تباطؤ مقصود ثانية بين كل رفع، ومع خطأ 429 ينتظر المهلة التي يطلبها ديسكورد ثم يكمل.
3. **حد 256KB للإيموجي** — الأكبر يُتخطّى بدل إسقاط العملية (كل الحزمة الحالية تحت الحد).

**تفاصيل التنفيذ:**
- تقرير تقدّم حيّ كل 5 إيموجيات، فالعملية الطويلة لا تبدو معلّقة
- منع تشغيل رفعتين على نفس السيرفر في وقت واحد
- الحذف يمس **إيموجيات الحزمة بالاسم فقط** — إيموجياتك الأخرى تبقى سليمة
- فحص صلاحية البوت (**إدارة التعابير**) لا صلاحية الأدمن فقط، وأزرار اللوحة تُعطَّل تلقائيًا عند الامتلاء أو غياب الصلاحية

**إصلاح:** ترتيب خيارات أمر الحذف خالف قاعدة ديسكورد (المطلوب قبل الاختياري) — كان الأمر سيفشل في التسجيل. اكتُشف بالاختبار.

**اختبارات:** 39 جديدة تغطي السقف والتوقف والبادئة وحد المعدل ومنع التزامن، ومستقرة عبر 5 تشغيلات.

---

## v26 — نظام السجن الكامل، وكل أنظمة المدينة داخل اللوحة

**قاعدة ثابتة من الآن:** أي نظام يُضاف مستقبلًا، يُضاف معه تحكّمه الكامل في `/لوحة`. هذا الإصدار يطبّقها بأثر رجعي على أنظمة v25 التي كانت بأوامر سلاش فقط.

**1) السجن يُخفي كل الرومات فعليًا**
سحب الرتب وحده لم يكن كافيًا — القنوات المرئية لـ@everyone تبقى ظاهرة. أُضيف **تهيئة صلاحيات السجن**: يضبط رتبة السجن على **كل قنوات السيرفر** دفعةً واحدة — منع الرؤية والإرسال والصوت في كل مكان، وسماح في الرومات المحددة فقط.
- تحديد الرومات الظاهرة من اللوحة (حتى 10، وتقبل كاتيغوري كاملة)
- زر **تهيئة الصلاحيات** يطبّقها، ويُبلغ بعدد ما أُخفي وما بقي وما تعذّر
- العملية تمس كل القنوات فتُؤجَّل (`deferUpdate`) تفاديًا لمهلة ديسكورد

**2) عفو عام**
`/عفو confirm:True` أو من اللوحة بزر — يُفرج عن كل المسجونين دفعة واحدة **مع استعادة رتب كل واحد**. اللوحة تعرض شاشة تأكيد بالعدد قبل التنفيذ.

**3) قسم "المدينة والسجن" في اللوحة**
- **السجن:** رتبة السجن • رومات السجن • تهيئة الصلاحيات • فك سجن عضو (بقائمة أسماء) • عفو عام
- **الكلبشة:** عرض المكلبشين • فك كل الكلبشات (الزر يُعطَّل تلقائيًا إن لم يوجد أحد)
- **الرتب العسكرية:** عرض السلّم
- **التفعيل:** تشغيل/إيقاف النظام، وتبديل "مطلوب بعد السرقة"

**إصلاح:** `timestamp` كانت مستخدمة في اللوحة بلا استيراد — كانت ستُسقط شاشتَي السجن والكلبشة عند أول فتح. اكتُشفت قبل التسليم.

**اختبارات:** 45 جديدة لشاشات اللوحة وآلية إخفاء الرومات، كلها ناجحة. وتحقّقت أن 12 نظامًا سابقًا ما زال يعمل.

---

## v25 — الشخصيات المتعددة والممتلكات والاعتقال والترقية الآلية

**تحليل خمسة بوتات خارجية، ونقل ما يستحق فقط. لم يُحذف أي شيء من المشروع.**

**نتيجة التحليل:**

| الملف | الحجم الحقيقي | القرار |
|---|---|---|
| لو تاون RP | 13,311 سطر | نُقلت منه 4 ميزات |
| الحماية القوية | 2,361 سطر | **مرفوض** — الاسم مضلل |
| فالكون العسكري | 1,541 سطر | نُقلت منه الترقية الآلية |
| عسكريه مطور | 640 سطر | مرفوض — أزرار `myButton1/2/506` |
| Falcon Gulf | 81 ملف | مرفوض — نسخ ولصق لنفس التذكرة |

**لماذا رُفض "الحماية القوية":** فُحص كل معالج فيه — كلها **تسجّل الحدث في قناة وتنتهي**، بلا حدود ولا عتبات ولا حظر تلقائي. نظام `security` عندنا فيه 7 قواعد بعقوبات فعلية، فهو أقوى. لم يُنقل منه شيء.

**تنبيه أمني:** أربعة من الملفات فيها توكنات بوتات مكشوفة. لم يُنسخ أي توكن أو معرّف للمشروع، وحُذفت ملفات التحليل بالكامل بعد الفراغ منها.

**جديد:**

**1) الشخصيات المتعددة (`/شخصية`)** — لكل عضو عدة شخصيات، كل واحدة بممتلكاتها المستقلة. التبديل ذرّي: شخصية نشطة واحدة مضمونة مهما تزامنت الضغطات (مُختبَر بـ30 تبديلًا متتاليًا).

**2) الممتلكات (`/ممتلكاتي`)** — مركبات بلوحات عشوائية وعقارات بمواقع، مع معرض للشراء (`/مدينة معرض`). المخزون ذرّي، والشراء المكرر لنفس الشخصية مرفوض بلا خصم مزدوج. لو نفدت الكمية بعد الخصم يُعاد المبلغ فورًا.

**3) الاعتقال (`/كلبشة`)** — أخف من السجن: يُجمّد أوامر RP بلا سحب رتب. مربوط بكل أوامر RP الموجودة (الوظائف، السوق، السرقة، البيع، تبديل الشخصية). أُضيف `/فك_كلبشة` و`/المكلبشين`.

**4) التفتيش والمصادرة (`/تفتيش`)** — كشف حقيبة عضو مع تمييز المواد الممنوعة، ومصادرة عنصر بسجل كامل. الإكمال التلقائي يقترح ما يملكه الهدف فعلًا لا كل الكتالوج.

**5) الترقية الآلية (`/عسكرية ترقية`)** — سلّم رتب بالنقاط: العضو يستلم رتبته حين تبلغ نقاطه الحد. الترقية تسحب رتب السلّم القديمة ولا تلمس رتب العضو الأخرى.

**توحيد:** فحص السجن في أوامر RP صار فحصًا شاملًا (`restrictionOf`) يشمل الكلبشة أيضًا، فلا تُنسى حالة عند إضافة أوامر جديدة.

**اختبارات:** 82 جديدة، كلها ناجحة، ومستقرة عبر 6 تشغيلات متتالية. وتحقّقت أن 16 نظامًا سابقًا ما زالت تعمل بعد التوسعة.

---

## v24 — أنظمة المدينة (GTA RP)

**أُضيفت فوق الأنظمة القائمة بلا حذف أي شيء. الدليل الكامل في `RP.md`.**

**ما كان موجودًا فطُوِّر بدل تكراره:** التصويت (`/انتخابات` + `/حكومة مجلس`)، المخالفات (`/مخالفة`)، السجلات، الصلاحيات، قاعدة البيانات، الأمان — كلها كانت مبنية، فبُنيت الأنظمة الجديدة فوقها. الرصيد يستخدم نظام البنك نفسه لا جدولًا منفصلًا، فيبقى مصدرًا واحدًا للحقيقة.

**جديد:**
- **العناصر والحقيبة** — كتالوج قابل للتعريف من الأوامر، حقيبة مستقلة لكل عضو مع Pagination، وسجل كامل لكل حركة. **لا يقدر عضو التنقّل في حقيبة غيره.**
- **الوظائف** (`/مدينة وظيفة-انشاء`) — رتبة لكل وظيفة، **وظيفة واحدة في كل مرة**، وجلسة عمل بمدة وتبريد ومكافأة عشوائية ضمن مدى.
- **البيع** (`/بيع`) — يسحب العنصر ذرّيًا ثم يضيف المال.
- **السوق السوداء** — لوحة بقائمة منتجات، وإن نفدت الكمية بعد الخصم **يُعاد المبلغ فورًا**.
- **السرقات** — نسبة نجاح، عنصر مطلوب (يُستهلك اختياريًا)، تبريد، و**شرط عدد الشرطة المباشرين يقرأ من نظام `/عسكرية` الموجود**.
- **السجن** — يحفظ الرتب ويستعيدها بدقة، ويمنع الوظائف والسوق والسرقة والبيع أثناءه. الحالة في القاعدة لا الذاكرة، **فتنجو من إعادة تشغيل البوت** (يُفحص المستحقون عند الإقلاع وكل دقيقة).
- **المطلوبون** — درجات خطورة، والسرقة الناجحة تجعله مطلوبًا تلقائيًا.

**تحسين للإطار كله:** أُضيف دعم **الإكمال التلقائي** (Autocomplete) في موجّه التفاعلات — لم يكن مدعومًا، فأي أمر يستخدمه كان سيفشل بصمت. الآن أي أمر يقدر يعرّف `autocomplete()`، وأخطاؤه تُرد بقائمة فارغة بدل انهيار.

**الحماية من التكرار:** كل عملية تُنقص كمية أو رصيدًا تضع شرطها داخل جملة SQL نفسها. مُختبَر بـ50 محاولة متزامنة لكل من: البيع، بدء العمل، استلام المكافأة، بدء السرقة، حسم السرقة — تنجح واحدة فقط في كل حالة.

**اختبارات:** 1048 ناجحة، صفر فاشلة (79 جديدة).

---

## v23 — تهيئة للاستضافة ذات لوحة التحكم

**بعد إلغاء Railway و GitHub والعودة للاستضافة السابقة، تأكّدت أن البوت يعمل عليها فعليًا بلا أي مكتبة تحتاج بناءً أصليًا.**

**تحقّق فعلي:** شغّلت البوت في بيئة بلا `better-sqlite3` وبلا `canvas` إطلاقًا — طُبّقت الهجرات الـ21 كلها، حُمّل 72 أمرًا، ونجحت الكتابة والقراءة والنسخ الاحتياطي. يعمل بالكامل على SQLite المدمج في Node 22.

**1) فحص إصدار Node عند الإقلاع**
اكتُشف تعارض: `engines` كان يقول Node ≥18، بينما البوت بلا `better-sqlite3` يحتاج **22** فعليًا. على استضافة بإصدار أقدم كان سينهار برسالة غامضة داخل طبقة قاعدة البيانات.
صار الفحص في أول سطر: يتوقف فورًا برسالة عربية تشرح الحل (`Startup ← Node Version ← 22`) — ولا يعترض إطلاقًا إن كانت `better-sqlite3` مثبّتة أو Node 22 موجودًا. وصُحِّح `engines` إلى `>=22`.

**2) دليل نشر جديد للاستضافة**
`DEPLOY.md` أُعيدت كتابته للوحات التحكم: الخطوات، حماية `data/`، الاستعادة من النسخ الاحتياطي، ورفع التحديثات بلا فقدان بيانات. دليل Railway محفوظ في `DEPLOY-RAILWAY.md` لو احتجته لاحقًا.

**ملاحظة:** ملفات `railway.json` و`nixpacks.toml` و`Procfile` و`.github/` تُتجاهَل تمامًا على استضافتك. تركتها للمستقبل، وحذفها آمن.

**اختبارات:** 969 ناجحة، صفر فاشلة.

---

## v22 — الهوية الوطنية والأنظمة الحكومية

**دمج ما يستحق من بوت "وولف ستي" بعد تحليل كامل. المصدر 6,513 سطر فقط (الباقي مكتبات)، وبوتنا أكبر منه بـ2.6 مرة ومعماريته أقوى — فنُقلت الأفكار المفيدة فقط، وأُعيدت كتابتها بمعاييرنا.**

**ما لم يُنقل ولماذا:** قاعدتهم MongoDB + JSON مبعثرة بلا عزل سيرفرات (`GuildID` ثابت في الإعدادات، وجدول الأرصدة مفتاحه `_id` بلا حقل سيرفر — يعني رصيد العضو مشترك بين كل السيرفرات)، و44 موضع `Database.get` ثم كتابة بلا حماية من حالات السباق، وصفر اختبارات. نقل معماريتهم كان سيكسر ما بنيناه.

**1) 🪪 الهوية الوطنية (`/هوية`)**
الميزة الوحيدة عندهم وما كانت عندنا — وهي على قائمة المؤجَّلات من v6.
- طلب هوية بنموذج (اسم، ميلاد، مكان، جنسية، مهنة) → مراجعة بأزرار → تسليم البطاقة في الخاص
- ترقيم متسلسل مستقل لكل سيرفر، وهوية واحدة لكل عضو (مفتاح فريد)
- رتبة مواطن تلقائية عند القبول، ومدة صلاحية اختيارية
- بحث في السجل المدني بالاسم أو الرقم
- **البطاقة تُعرض بطريقتين:** صورة مرسومة إن ثُبّتت مكتبة الرسم وحُدِّد قالب، أو **بطاقة منسّقة تعمل فورًا بلا أي إعداد**. اخترنا هذا لأن مكتبة `canvas` تحتاج بناءً أصليًا قد يفشل على بعض الاستضافات — فلا يصح تعطيل النظام كله بسببها.
- **كل شيء قابل للتخصيص لاحقًا:** `/هوية قالب` لرفع صورة القالب وضبط مواضع النص وأحجامه وألوانه بصيغة JSON، بلا لمس الكود.

**2) 🏛️ مجلس الشورى (`/حكومة مجلس`)**
يختلف عن `/انتخابات` الموجود: هناك تختار مرشحًا، وهنا تصوّت **موافق/رافض/ممتنع** على *مشروع قرار* مقترح، ثم يتخذ الرئيس القرار النهائي مع ميقات الجلسة. تغيير الصوت يستبدل السابق بلا تكرار، والقرار النهائي ذرّي.

**3) 📢 التعاميم الإدارية (`/حكومة تعميم`)**
تعاميم رسمية مرقّمة بجهة مُصدِرة، تُنشر في قناة مخصصة.

**4) 🎗️ التقاعد (`/حكومة تقاعد`)**
يختلف عن الاستقالة الموجودة: إحالة مشرّفة مع كلمة تكريم، وحساب مدة الخدمة من تاريخ الانضمام، وحفظ الرتب المسحوبة في السجل. يحترم الرتب المعفاة وتسلسل رتبة البوت.

**اختبارات:** 969 ناجحة، صفر فاشلة (80 جديدة).

---

## v21 — إصلاح "did not respond"، واللوحة كلها بالشكل الحديث

**كلها إصلاحات لأخطاء ظهرت في الاستخدام الفعلي.**

**1) إصلاح `The application did not respond`**
موجّه التفاعلات كان ينادي المعالج **بلا `try/catch`**، فأي استثناء أو تأخّر يتجاوز مهلة ديسكورد (3 ثوانٍ) يترك المستخدم بلا رد. أُضيفت شبكة أمان تلتقط أي فشل وتردّ برسالة واضحة مع تسجيل الخطأ للمطوّر. اكتُشف الخلل من صورتك، وثبت أثناء الاختبار حين انهارت شاشة الإحصائيات — فالتقطتها الشبكة بدل أن تصمت.

**2) اللوحة كلها صارت Components v2 — الأزرار داخل الحاوية**
كانت أزرار اللوحة تظهر **تحت** الإمبيد بالشكل القديم. الآن كل الشاشات (30 شاشة) تمر عبر محوّل موحّد يجعلها حاوية واحدة والأزرار بداخلها.
السبب في التحويل الشامل لا الجزئي: ديسكورد **لا يسمح بإزالة علم v2 بعد ضبطه** على رسالة، واللوحة تتنقّل على نفس الرسالة — فإما كل الشاشات v2 أو كلها تقليدية. اختبار جديد يتحقق أن **كل** شاشة ترجع v2، فلا تتسرّب شاشة قديمة لاحقًا وتكسر التنقّل.

**3) الإمبيدات الجديدة تبدأ بالشكل الحديث**
كنت تنشئ إمبيدًا فتطلع أزراره منفصلة تحته. الآن الإمبيد الجديد يبدأ بـ v2 مباشرة. الإمبيدات القديمة تبقى على شكلها حتى لا تنكسر رسائلها المنشورة، وزر **الشكل** يبدّل بينهما وقت ما تبي.

**4) السلم الإداري يُدار من اللوحة بلا أي أمر Slash**
كانت الشاشة تقول "استخدم `/staff-rank add`" — وهذا يناقض فكرة اللوحة. الآن فيها:
**➕ إضافة رتبة** (باختيار الرتبة من قائمة) • **🗑️ حذف رتبة** • **🎖️ رتبة الطاقم الأساسية**
مع رفض واضح للرتبة المكررة.

**اختبارات:** 889 ناجحة، صفر فاشلة (11 جديدة).

---

## v20 — تعديل الأزرار والقوائم، ودليل الإجراءات

**المشكلة:** كان لازم تحفظ صيغ الإجراءات عن ظهر قلب، ولو غلطت في زر ما فيه إلا حذف كل المكوّنات وإعادتها من الصفر.

**جديد:**

**1) 📖 زر «الإجراءات المتاحة»**
يعرض كل ما هو موجود **فعليًا في سيرفرك الآن** جاهزًا للنسخ: أنواع تذاكرك بأسمائها، أنواع التقديم، **الرحلات المفتوحة برموزها**، إمبيداتك الأخرى، وكل إجراءات الأنظمة مع شرح كل واحد. ما عاد فيه حفظ ولا تخمين.

**2) ✏️ تعديل مكوّن موجود**
تختار الزر أو القائمة (وتشوف اسم كل واحد ووش يسوي)، فتفتح نافذة **معبّأة بقيمه الحالية** — تعدّل وتحفظ. للقوائم، خياراتها تطلع مكتوبة بالصيغة الجاهزة للتحرير.
التحقق يشتغل عند التعديل زي الإنشاء: إجراء غير صالح يُرفض ويُشرح السبب، والمكوّن القديم يبقى سليمًا بلا تخريب.

**3) `sys:flight:join:<الرمز>` — الانضمام لرحلة محددة**
كان `flight:book` يفتح قائمة كل الرحلات. الآن تقدر تسوي زر مربوط **برحلة بعينها**: من يضغطه ينحجز فيها مباشرة بلا اختيار. مفيد لما تنشر إعلان رحلة وتبي زر "انضم" تحته.
رمز الرحلة يُتحقَّق منه **وقت إنشاء الزر** لا وقت الضغط، فما تنشر زرًا لرحلة غير موجودة.

**تحسين بنيوي:** نظام إجراءات `sys:` صار يدعم الوسائط (`إجراء:وسيط`) بشكل عام، فأي إجراء مستقبلي يحتاج معرّفًا محددًا يستفيد من نفس الآلية بلا تغيير في المحرّك.

**اختبارات:** 898 ناجحة، صفر فاشلة (34 جديدة).

---

## v19 — النظام العسكري / الشرطي

**ثلاثة أنظمة مترابطة في وحدة واحدة (`/عسكرية`)، بأوامر فرعية عربية بالكامل.**

**1) نقاط العسكريين**
- إضافة وإزالة وتحديد وتصفير شامل، مع **سجل كامل** لكل عملية (من نفّذ، كم، ولماذا) للمساءلة
- النقاط لا تنزل تحت الصفر مهما كان مقدار السحب
- كشف الصدارة بميداليات، أو استعلام عن نقاط عسكري محدد
- ملاحظة: هذا نظام منفصل تمامًا عن `staff_activity` الذي يقيس النشاط تلقائيًا — هنا النقاط تُمنح يدويًا من المسؤولين

**2) مركز العمليات (الدوام)**
- لوحة دائمة بالشكل الحديث: تسجيل دخول • تسجيل خروج • كشف المباشرين
- **نوبة واحدة مفتوحة لكل عسكري مضمونة ذرّيًا** — 50 ضغطة متزامنة على "تسجيل دخول" لا تفتح إلا نوبة واحدة
- رتبة دوام اختيارية تُمنح عند الدخول وتُسحب عند الخروج، فتكون الحالة مرئية في قائمة الأعضاء
- سجل دخول/خروج بقناة مخصصة يعرض المدة والنقاط
- عدّاد المباشرين في اللوحة يتحدّث تلقائيًا مع كل دخول وخروج
- `/عسكرية دوامي` يعرض إجمالي ساعات الأسبوع والإجمالي الكلي وآخر النوبات

**3) البلاغات**
- لوحة بلاغات للمواطنين بزر واحد → اختيار نوع البلاغ (سرقة/خطف/قتل/اعتداء/مطاردة/أخرى) → نموذج تفاصيل وموقع ومشتبه به
- ترقيم متسلسل مستقل لكل سيرفر
- **استلام ذرّي**: أول عسكري يضغط هو من يستلم، و50 ضغطة متزامنة لا ينجح منها إلا واحدة
- المُبلِّغ يوصله إشعار في الخاص عند استلام بلاغه، كما هو موعود في اللوحة
- منشن اختياري لرتبة العسكر عند وصول بلاغ جديد

**الإعداد:** `/عسكرية اعدادات` لضبط الرتبة العسكرية، رتبة الدوام، سجل الدوام، قناة البلاغات، ومنشن البلاغات — مع التحقق المسبق من صلاحيات البوت وتسلسل الرتب قبل الحفظ.

**اختبارات:** 864 ناجحة، صفر فاشلة (75 جديدة للنظام العسكري).

---

## v18 — الانتخابات بمرشحين والمنشورات الاجتماعية

**إكمال الميزتين المتبقيتين من الجولة السابقة، بنفس معايير العزل والذرّية والاختبار.**

**جديد:**

**1) نظام الانتخابات بمرشحين (`/انتخابات`)**
- دورة حياة كاملة: تسجيل → تصويت → إقفال، كل مرحلة تُقفل السابقة ذرّيًا (لا رجوع بلا قرار إداري صريح)
- تسجيل ترشّح ببرنامج انتخابي، يصل قناة مراجعة بأزرار قبول/رفض — القرار ذرّي، لا يُبت في نفس المرشح مرتين
- تصويت أحادي (افتراضي) أو متعدد (اختياري لكل انتخاب)، كلاهما محميان ذرّيًا من التصويت المزدوج
- لوحة دائمة بالشكل الحديث (Components v2) تتغيّر تلقائيًا حسب المرحلة: أزرار تسجيل/تصويت في وقتها فقط
- نتائج مباشرة بشريط تقدّم ونسب مئوية، وميدالية للفائز عند الإقفال

**2) المنشورات الاجتماعية (`/تغريدة`)**
- ملف شخصي اختياري بمعرّف فريد (handle) واسم ظاهر ونبذة
- نشر، رد، إعجاب (بالتبديل)، وإعادة نشر — كل بطاقة منشور Components v2 حقيقية بأزرار تفاعل حقيقية
- نظام إبلاغ يصل قناة مراجعة بزر حذف فوري للطاقم؛ صاحب المنشور نفسه يقدر يحذفه أيضًا
- صدارة الأكثر نشاطًا أو الأكثر إعجابًا
- نطاق مقصود أن يبقى بسيطًا: بلا خوارزميات ترشيح ولا أرقام أرباح وهمية — امتداد ترفيهي لسيرفر إدارة، لا منصة مستقلة

**إصلاح خلل توقيت (نفس فئة الخلل المكتشف سابقًا في v17، وُجد ثانية بالمصادفة أثناء اختبار المنشورات):**
ترتيب الخلاصة (`feed`)، منشورات العضو (`byAuthor`)، والردود (`replies`) كانت تعتمد على `created_at` (دقة مللي ثانية). نفس السيناريو: منشوران بنفس المللي ثانية → ترتيب غير محدَّد. صُحِّح بالاعتماد على `id` (متسلسل، لا يتساوى أبدًا) بدل `created_at` في كل الاستعلامات التي يهم ترتيبها الدقيق. طُبِّق نفس الإصلاح احتياطيًا على `listCandidates` في الانتخابات رغم عدم ظهور المشكلة فعليًا هناك في 15 تشغيلة اختبار متتالية، لأنه نفس نمط الخطر بالضبط.

**اختبارات:** 789 ناجحة، صفر فاشلة (69 جديدة للانتخابات، 69 جديدة للمنشورات الاجتماعية — من أصل 720 سابقة).

---

## v17 — إيقاف الخدمات البنكية، لوحة بنكية حديثة، وتقديمات هرمية

**تحليل صور مرجعية لبوت آخر لمعرفة أي ميزة تستاهل النقل. النتيجة: معظم الأنظمة المطلوبة كانت موجودة أصلًا (بنك، طيران، تقديمات)، فالعمل تركّز على الفجوات الحقيقية الثلاث فقط — بلا نسخ حرفي وبلا إعادة كتابة لما هو شغّال أصلًا.**

**جديد:**

**1) إيقاف/تفعيل الخدمات البنكية**
- عمود `frozen` على الحساب، بشرط `frozen = 0` **ذرّي داخل جملة SQL نفسها** لكل من التحويل والإيداع والسحب — لا فحص منفصل قابل لحالة سباق
- `/ادارة_بنك freeze` و`/ادارة_بنك unfreeze` (سبب اختياري، إشعار خاص للعضو)
- الاستلام يبقى ممكنًا لحساب مجمّد (لا يُعاقب أحد لأنه استلم تحويلًا)، و`charge` (خصم العقوبات) يعمل رغم التجميد عمدًا — التجميد لا يُعفي من المخالفات
- كشف الحساب يعرض الحالة الموقوفة والسبب بوضوح ولون أحمر

**2) لوحة بنكية بالشكل الحديث (Components v2)**
- `/بنك panel channel:#القناة` تنشر لوحة دائمة بالأزرار **داخل** الحاوية الملوّنة (فتح حساب، سحب، إيداع، كشف حساب، تحويل)
- كل زر يستخدم نفس محرك `sys:bank:*` الموجود أصلًا — صفر منطق مكرر
- زر «فتح حساب» جديد (`bank:open`) يوضّح للعضو أن حسابه جاهز

**3) تقديمات هرمية (فئة رئيسية ← نوع فرعي ← استمارة)**
- عمود `category` اختياري على أنواع التقديم؛ الأنواع بلا فئة تبقى تعمل كما هي بلا أي تغيير
- `/تقديم create ... category:وزارة الداخلية` لتصنيف الأنواع
- `/تقديم panel channel:#القناة` تنشر لوحة تصفّح بالشكل الحديث: قائمة فئات ← قائمة أنواع الفئة ← نفس نموذج التقديم الأصلي بلا أي تعديل عليه
- فئة بلا أنواع مفعّلة تختفي تلقائيًا من القائمة

**ما لم يُبنَ (خارج النطاق أو بانتظار قرارك):**
- نظام محاكاة "تويتر" داخل ديسكورد (منشورات، متابعون، أرباح) — منصة اجتماعية كاملة مستقلة عن إدارة السيرفر، لم يُبنَ لعدم وضوح الحاجة الفعلية له
- نظام تصويت بمرشحين وبطاقات ترشّح — أوسع من الاستطلاع العام الحالي، بانتظار تأكيدك

**إصلاح خلل توقيت (اكتُشف أثناء اختبار الفئات):** `listCategories`/`listByCategory`/`listTypes` كانت تُرتّب بـ `created_at` (دقة مللي ثانية)، فلو أُنشئ نوعان بنفس المللي ثانية (سيناريو شائع في الاستيراد الجماعي أو الاختبارات) يصير الترتيب غير محدَّد. صار الترتيب بـ `rowid` الذي لا يتساوى أبدًا. اكتُشف بفحص تكراري (20 تشغيلة) بعد ملاحظة نتيجة متذبذبة لاختبار واحد.

**اختبارات:** 676 ناجحة، صفر فاشلة (69 جديدة: 38 للتجميد البنكي، 31 للتقديمات الهرمية).

---

## v16 — الشكل الحديث للإمبيدات (الأزرار داخل الحاوية)

**المشكلة:** الإمبيدات المبنية بـ `/embed` كانت تظهر بالشكل التقليدي — الأزرار منفصلة **تحت** الإمبيد، بينما المطلوب أن تكون **داخل** الحاوية الملوّنة نفسها كما في السيرفرات الحديثة.

**جديد:**
- **زر «الشكل» في محرّر الإمبيد** — يبدّل بين:
  - 📋 **تقليدي** — إمبيد عادي والأزرار تحته (الافتراضي، للتوافق مع الإمبيدات الموجودة)
  - 🆕 **حديث** — حاوية ملوّنة واحدة، **الأزرار والقوائم داخلها**، والصور معرض وسائط مدمج
- **زر «معاينة حية»** — يعرض الشكل النهائي بالضبط قبل النشر (رسالة مخفية منفصلة، لأن ديسكورد يمنع خلط النمطين في رسالة واحدة)
- التبديل يشمل كل شيء تلقائيًا: الإرسال لقناة، «تحديث المرسل»، والأزرار التي تفتح إمبيدات أخرى
- المتغيرات (`{user}`, `{server}`...) والحقول والفوتر تعمل في النمطين

**إصلاح مهم:** `CommandContext.reply()` كان يستبدل أعلام الرسالة بعلم الإخفاء (`flags = 64`) بدل دمجها، ما كان سيُلغي علم Components v2 عند أي رد مخفي. صار الدمج بعملية OR.

**قرار مقصود — لماذا لوحة `/devpanel` بقيت تقليدية:**
ديسكورد **لا يسمح بإزالة علم Components v2 بعد ضبطه على رسالة**. واللوحة تتنقّل عبر 30 شاشة فرعية على **نفس الرسالة** بـ `interaction.update()`. لو حوّلنا الشاشة الرئيسية فقط للنمط الحديث، لانكسر التنقّل لكل الشاشات التي تعرض إمبيدات. الإمبيدات التي تبنيها أنت بـ `/embed` لا تعاني من هذا لأن كل واحدة رسالة مستقلة — فالنمط الحديث متاح فيها بالكامل.

**اختبارات:** 607 ناجحة، صفر فاشلة (20 جديدة تغطي النمطين والتبديل بينهما والمتغيرات والحالات الفارغة).

---

## v15 — الفواتير، حاسبة الضريبة، ومركز الخدمات

**دمج مفاهيمي من بوت خدمات خارجي (مُقدَّم من المستخدم لتحليله ونقل ما يفيد منه). لم يُنسخ أي كود حرفيًا — كل ميزة أُعيد بناؤها من الصفر داخل معمارية المشروع، وحُذف كل أثر لهوية السيرفر الأصلي ومعرّفاته وتوكنه قبل أي عملية دمج.**

**جديد:**
- **`Components v2`** (`src/core/utils/componentsV2.js`) — أداة بناء رسائل بالواجهة الحديثة (`ContainerBuilder`, `MediaGalleryBuilder`, فواصل تلقائية)، تُستخدم حاليًا في عرض الفواتير، مع الإبقاء على `buildEmbed` كافتراضي لبقية البوت لأنه أخف وأنسب لمعظم الحالات
- **`parseAmount()`** (`core/utils/common.js`) — يحوّل نصوص مبالغ مختصرة (`10k`, `1.5m`, `1b`, وما يقابلها بالعربي: `ألف`, `مليون`, `مليار`) إلى رقم صحيح، بمعالجة صارمة للمدخلات غير الصالحة
- **`/فاتورة`** — نظام فوترة كامل للبائعين: `create` (عميل + منتج + مبلغ + طريقة دفع)، `list`، `stats`، `channel`. الفاتورة تُبنى بـ Components v2 وتُنشر تلقائيًا، والترقيم متسلسل وذرّي لكل سيرفر (نفس آلية القضايا والتذاكر)
- **`/بنك tax`** و**حاسبة الضريبة في `/خدمات`** — دالة واحدة مصدر للحقيقة (`EconomyService.calculateTax`) تُستدعى من الأمر المباشر ومن مركز الخدمات، بنسبة ضريبة قابلة للتعديل لكل سيرفر (`economy.taxPercent`, افتراضي 5.26%)
- **`/خدمات`** — مركز خدمات بقائمة اختيار واحدة (تقييم / اقتراح / حاسبة ضريبة) تفتح مودال مناسب لكل خيار، مع قنوات استقبال منفصلة قابلة للتخصيص (`logs.ratings`, `logs.suggestions`)

**قرارات مقصودة أثناء الدمج (لا نسخ حرفي، تحسين معماري لا إعادة إنتاج للعيوب):**
- المصدر يستخدم 7 نسخ منسوخة يدويًا من نفس منطق التذاكر، وحالة سباق حقيقية عند الاستلام، وخطأ فعلي في فحص الصلاحيات (`Collection.has()` بعدة معاملات لا يفحص إلا أولها) — ولا شيء من هذا نُقل؛ الفواتير مبنية بترقيم ذرّي حقيقي داخل معاملة واحدة
- حاسبة الضريبة نسبتها **قابلة للتعديل لكل سيرفر** بدل رقم ثابت داخل الكود
- مركز الخدمات يستدعي نفس دالة الاقتصاد الحقيقية بدل تكرار صيغة الحساب في مكانين مختلفين قد يختلفان لاحقًا

**اختبارات:** 668 ناجحة، صفر فاشلة (81 جديدة: 14 لـ Components v2، 42 للفواتير وحاسبة الضريبة، 25 لمركز الخدمات).

---

## v14 — لوحة التحكم المركزية (/devpanel)

**الهدف: تقليل الاعتماد على أوامر Slash منفصلة لكل إعداد، وتوحيد الإدارة داخل لوحة واحدة.**

**جديد:**
- **`/devpanel`** — اسم بديل (alias) لأمر `/لوحة` الموجود، ونطاقه وُسِّع ليغطي كل الأنظمة الإدارية
- 12 نظامًا جديدًا أُضيفت للوحة (كانت تُدار بأوامر منفصلة فقط): التذاكر، الإجازات/الاستقالات/البلاغات، التقارير الدورية، التفاعل والمحتوى (الردود التلقائية، رد التفاعلات، لوحة النجوم، المميّز)، الدلائل، الحماية، مراقبة السيرفرات (للمطور)، الفحص الشامل
- التنقّل بالكامل عبر `interaction.update()` — تعديل نفس الرسالة، بلا رسائل جديدة في كل خطوة
- فحص الصلاحية **عند كل تفاعل** داخل اللوحة (زر، قائمة، مودال) لا فقط عند فتحها — نفس محرك الصلاحيات المركزي الموجود
- استخدام Modals لإعدادات الإغلاق التلقائي وحد الاستلام والبحث عن سيرفر، وSelect Menus للقنوات والرتب

**قرارات مقصودة:**
- **لا حذف ولا كسر لأي أمر موجود** — الـ65 أمرًا كلها سليمة، والأوامر الفردية (`/رد_تلقائي create`, `/دليل add`...) بقيت كما هي لأن كل قاعدة/دليل له اسم مستقل يصعب إدارته بأزرار عامة؛ اللوحة تدير التبديل والقنوات فقط
- لا نظام جديد أُضيف، ولا منطق أعيدت كتابته — اللوحة طبقة تحكم فوق الخدمات الموجودة (`app.ticketService`, `app.lifecycleService`, `app.reportService`...) فقط
- زر `/مساعدة` داخل اللوحة يوجّه لأمر المساعدة الكامل بدل تكرار نفس المحتوى

**إصلاح:** استيراد ناقص لدالة `truncate` في وحدة اللوحة، اكتُشف بالاختبار قبل الوصول للإنتاج.

**اختبارات:** 587 ناجحة، صفر فاشلة (29 جديدة تغطي كل شاشة وفحص صلاحية).

---

## v13 — إكمال دمج c2 بالكامل

**المراحل 4 و5 الأخيرتان. كل ما في نظام c2 المفيد أصبح جزءًا من البوت.**

**جديد:**
- **رد التفاعلات** (`/reactionreply`): الضغط على إيموجي معيّن على أي رسالة يعطي رتبة (بالتبديل) أو رداً نصياً أو إمبيداً، بقيود قنوات اختيارية
- **المنشورات المميّزة** (`/featured`): تمييز رسالة بأمر أو **بالزر الأيمن ← تطبيقات ← تمييز الرسالة**، تُنشر في قناة مخصصة مع رابط للأصل. التمييز المكرر لنفس الرسالة مرفوض ذرّيًا
- **سجل الإصدارات** (`/changelog`): المطور ينشر تحديثاً فيبثّه تلقائياً لكل سيرفر مشترك، مع تتبّع من استلم أي إصدار فلا يتكرر الإرسال
- **ربط GitHub** (`/github`): Webhook حقيقي يستقبل push وpull request وissues وreleases وينشرها كإمبيدات، **بتحقق HMAC-SHA256 من التوقيع** (نفس آلية GitHub الرسمية) عبر نفس خادم `/health` الموجود أصلاً — بلا منفذ إضافي ولا مكتبة خارجية

**قرارات أمنية:**
- توقيع Webhook يُتحقق منه بـ `crypto.timingSafeEqual` لمنع هجمات قياس التوقيت، لا بمقارنة نصية عادية
- كل سيرفر له سر (secret) مستقل — تسريب سر واحد لا يعرّض سيرفرات أخرى
- فلترة اختيارية بالمستودع تمنع استقبال أحداث من مستودعات غير مقصودة

**اختبارات:** 558 ناجحة، صفر فاشلة (37 جديدة)

**بهذا اكتمل دمج نظام c2 بالكامل — راجع `MERGE-PLAN.md` للملخص النهائي.**

---

## v12 — التقارير الدورية وتوثيق العقوبات

**إكمال المرحلة 3 + المرحلة 4 من دمج c2.**

**جديد:**
- **التقارير الدورية**: تقرير يومي أو أسبوعي يُرسل تلقائيًا لقناة محددة
  - ستة أقسام قابلة للاختيار: التذاكر • نشاط الطاقم • التقييمات • الإجراءات • التقديمات • غير النشطين
  - صدارة الرسائل والوقت الصوتي والتذاكر المستلمة
  - التشغيل يُسجَّل بمفتاح اليوم، فإعادة تشغيل البوت لا تُرسل التقرير مرتين
  - `/report-schedule now` لإرسال تقرير فوري بأي فترة
- **تنبيه انخفاض النشاط**: `/report-schedule inactive` يكشف من لم يبلغ الحد الأدنى من الرسائل
- **نظام الدلائل** (`/evidence`): توثيق العقوبات بالأدلة
  - نوع العقوبة، السبب، المدة، المكان، وحتى 10 روابط أدلة
  - ربط اختياري بالقضية الإدارية، وأول صورة تظهر داخل الإمبيد
  - ترقيم مستقل لكل سيرفر، ونشر في قناة الدلائل، وحذف يزيل رسالة النشر معه

**اختبارات:** 521 ناجحة، صفر فاشلة (42 جديدة)

---

## v11 — دمج المرحلة 3: تحسينات التذاكر

**أفضل ما في نظام تذاكرهم (13,000 سطر)، مُعاد كتابته داخل معماريتنا.**

**جديد:**
- **ترقيم التذاكر**: رقم متسلسل مستقل لكل سيرفر (`ticket-0220`)، يظهر في اسم القناة والأرشيف والتقييم
- **تقييم الخدمة**: بعد الإغلاق يصل صاحب التذكرة طلب تقييم في الخاص بخمسة أزرار نجوم + ملاحظة اختيارية
  - تقييم واحد لكل عضو لكل تذكرة (مفتاح فريد يمنع التكرار)
  - `/ticket-rating` لمتوسط كل إداري ولوحة الصدارة
- **الإغلاق التلقائي عند الخمول** على مرحلتين: تنبيه في التذكرة، ثم قفل أو حذف مع أرشفة بعد المهلة
  - أي رسالة تُلغي التنبيه وتُصفّر العدّاد
- **حد التذاكر المستلمة** لكل موظف في نفس الوقت، لمنع الاحتكار
- **ترانسكربت HTML** بدل النصي: تصميم داكن، اتجاه عربي، صور مضمّنة، وتهريب كامل لمنع XSS
- **`/ticket-settings`** لضبط كل ما سبق من ديسكورد

**قرارات تصميم:**
- الإغلاق التلقائي على مرحلتين عمدًا — إغلاق تذكرة نشطة بالخطأ أسوأ من تركها ساعة إضافية
- الحذف التلقائي يرفض العمل بلا قناة أرشيف، فلا تضيع محادثة
- تقييم التذكرة يعمل في الخاص، والموجّه يسمح بذلك استثناءً لهذا المعالج فقط

**اختبارات:** 479 ناجحة، صفر فاشلة (43 جديدة)

---

## v10 — مراقبة السيرفرات وحماية البوت

**جديد:**
- **`/guilds`** — لوحة مراقبة السيرفرات للمطورين: قائمة مرتّبة بالأعضاء أو النشاط أو الاستخدام، تفاصيل أي سيرفر، إحصائيات عامة، ورابط الدعوة
- **قائمة سوداء للسيرفرات**: الحظر يمنع البوت من العمل فيه ويغادره تلقائيًا، وحتى لو أُعيدت دعوته يغادر فورًا
- **حارس الإساءة** — الحماية الفعلية للاستضافة:
  - حد أوامر لكل عضو ولكل سيرفر في الدقيقة
  - تجاوز الحد → تبريد صامت (الرد نفسه يستهلك موارد)
  - تكرار التجاوز → حظر تلقائي ومغادرة، مع إشعار المطورين في الخاص
  - المطورون معفيّون حتى لا يُقفل البوت أثناء الصيانة
- **`/lockdown`** — إغلاق طارئ يقفل كل القنوات عند هجوم إغراق، **قابل للعكس تمامًا**: حالة كل قناة تُحفظ قبل القفل وتُستعاد بدقة (المقفلة تبقى مقفلة، والموروثة ترجع موروثة)، مع خيار إبقاء قناة مفتوحة للتواصل
- إشعار المطورين في الخاص عند انضمام البوت لأي سيرفر جديد
- تتبّع تلقائي لكل السيرفرات: الأعضاء، المالك، تاريخ الانضمام، وعدد الأوامر المنفّذة

**ما لم يُنفَّذ ولماذا:** طُلب أمر يحظر كل أعضاء سيرفر ويقفل رومه. لم أنفّذه — راجع `SECURITY.md`. البديل المنفّذ أعلاه يحمي البوت والاستضافة فعليًا بلا إضرار بأعضاء أبرياء ولا مخالفة سياسات ديسكورد.

**اختبارات:** 436 ناجحة، صفر فاشلة (44 جديدة)

---

## v9 — الردود التلقائية + جاهزية Railway

**دمج المرحلة 2 من c2 + بنية النشر الكاملة.**

**أنظمة جديدة:**
- **الردود التلقائية**: رد على كلمات مفتاحية بنص أو إمبيد مصمّم
  - 4 أنواع مطابقة: يحتوي على • كلمة مستقلة • مطابق تمامًا • يبدأ بـ
  - **توحيد النص العربي**: يتجاهل التشكيل ويوحّد الهمزات والتاء المربوطة والألف المقصورة
  - شروط: قنوات مسموحة/مستثناة، رتب مطلوبة، تبريد لكل عضو، نسبة رد
  - `/autoreply test` لتجربة نص ومعرفة أي رد سيُطابق قبل النشر
- **لوحة النجوم**: الرسائل التي تتجاوز عدد نجوم محدد تُنشر تلقائيًا، والعدّاد يتحدّث ويُحذف عند النزول تحت الحد

**جاهزية التشغيل:**
- **`/checkup`** — فحص شامل يكشف: أنظمة مفعّلة بلا إعدادات، قنوات ورتب محذوفة، صلاحيات ناقصة، رتب أعلى من البوت، إمبيدات محذوفة مرتبطة بردود وأوامر
- **خادم فحص صحة** على `/health` بلا أي مكتبة إضافية (`node:http`)
- **نسخ احتياطي دوري** لقاعدة البيانات مع تدوير تلقائي
- ملفات النشر: `railway.json` • `nixpacks.toml` • `Procfile` • GitHub Actions
- دليل `DEPLOY.md` كامل للنشر على Railway مع GitHub

**إصلاحات:** تصادم أسماء النسخ الاحتياطية عند إنشاء نسختين في نفس الثانية.

**اختبارات:** 392 ناجحة، صفر فاشلة (53 جديدة)

---

## v8 — دمج المرحلة 1: دورة حياة الطاقم

**مأخوذة من نظام c2 وأُعيدت كتابتها بالكامل داخل معماريتنا** (عزل لكل سيرفر + قرارات ذرّية + اختبارات).

**جديد:**
- **نظام الإجازات**: طلب بمدة وسبب، مراجعة بأزرار، وعند القبول **تُحفظ الرتب المسحوبة في السجل** وتُستعاد بدقة عند الانتهاء
- إنهاء تلقائي عند انتهاء المدة، أو يدوي بـ `/leave end`
- رتب معفاة من السحب، ورتبة إجازة تُعطى وتُسحب تلقائيًا
- **نظام الاستقالات**: طلب بسبب، وعند القبول تُسحب رتب الطاقم وتُحفظ للسجل
- **البلاغات على الإداريين**: بلاغ بالأدلة والشهود والمكان والوقت، ومراجعة بأزرار
- **تحذيرات متصاعدة تلقائيًا**: كل بلاغ مقبول يرفع الدرجة (1 → 2 → 3) ويعطي الرتبة المقابلة
- عند بلوغ الدرجة الأخيرة تُبلَّغ الإدارة العليا في قناتها
- مهلة بين البلاغات لمنع الإغراق، وقبول الروابط الآمنة فقط في الأدلة
- ترقيم متسلسل **مستقل لكل نوع ولكل سيرفر**

**إصلاحات أثناء البناء:** خطأ في مفتاح الإعدادات (`reports` مقابل `report`) كان يُسقط قبول أي بلاغ — اكتُشف بالاختبار وأُصلح.

**اختبارات:** 339 ناجحة، صفر فاشلة (54 جديدة)

---

## v7 — محرّر التقديمات التفاعلي

**جديد:**
- **محرّر تفاعلي كامل** لأنواع التقديم: `/application edit name:X` يفتح لوحة بأزرار فيها **معاينة حية** للطلب
- تعديل **الأسئلة سؤالاً سؤالاً**: إضافة، تعديل، حذف، وإعادة ترتيب — بدل استبدالها كلها دفعة واحدة
- **زخرفة كاملة** لإمبيد الطلب: عنوان بمتغيرات، لون، صورة كبيرة، صورة مصغّرة، فوتر، وإيموجي الحقول
- **نظام منشن**: حتى 5 رتب + `@everyone` + `@here` بأزرار تبديل، يُرسل مع الطلب لقناة المراجعة
- اختيار قناة المراجعة والرتب من قوائم، مع **تحقق مسبق** من صلاحيات البوت وتسلسل الرتب
- زر **نشر لوحة التقديم** ينشر إمبيد بزر تقديم جاهز في أي قناة
- فتح وإغلاق التقديم بضغطة، وحذف النوع بتأكيد
- `/application set` للتعديل السريع لخاصية واحدة بدون فتح المحرّر

**اختبارات:** 285 ناجحة، صفر فاشلة (40 جديدة)

---

## v6 — التقديمات واختبار التفعيل

**جديد:**
- **نظام التقديمات**: أنواع تقديم لكل قسم (عصابات، داخلية، إعلام...) بأسئلة وقناة مراجعة ورتبة قبول
- طلبات مرقّمة تُنشر في قناة المراجعة بأزرار **قبول / رفض / رفض بسبب**
- القرار ذرّي: لا يمكن قبول ورفض نفس الطلب معًا، والرسالة تتحدّث لتعرض القرار واسم المراجع
- إعطاء الرتبة تلقائيًا عند القبول، وسحب رتبة أخرى إن أردت، وإشعار العضو في الخاص
- **مهلة إعادة التقديم** بعد الرفض
- **أسئلة في الخاص** سؤالاً بعد سؤال (`سؤال 1/4`) — تتجاوز حد المودال (5 أسئلة) وتقبل الصور
- أسئلة رقمية فقط، وأسئلة نصية طويلة، وسؤال يطلب صورة
- **اختبار التفعيل**: بنك أسئلة نعم/لا، أزرار متسلسلة، درجة نجاح، ورتبة تُعطى تلقائيًا عند الاجتياز
- إجراءات جديدة للوحات: `apply:اسم_النوع` و `quiz`
- ترقيم إصدارات للملف + هذا السجل

**اختبارات:** 245 ناجحة، صفر فاشلة (52 جديدة)

---

## v5 — أنواع التذاكر والقوائم المتداخلة

- أنواع تذاكر: كاتيغوري ورتبة دعم وقالب اسم قناة وإمبيد ترحيب لكل نوع
- نموذج أسئلة قبل فتح التذكرة، والإجابات تُنشر للطاقم
- حد تذاكر مستقل لكل نوع
- قوائم متداخلة بلا حد + خيار رجوع للوحة الأم
- إجراء `ticket:اسم_النوع`
- دليل `PANELS.md`

---

## v4 — الاقتصاد

- **البنك**: رصيد نقدي ومصرفي، تحويل برسوم وسقف، إيداع، سحب، سجل حركات دائم
- **المخالفات**: ترقيم متسلسل، سداد يخصم فعليًا، رتبة إيقاف خدمات تلقائية
- **الطيران**: رحلات بسعر وسعة، حجز يخصم الثمن، إلغاء يسترجع
- 13 إجراء `sys:` تربط أزرار اللوحات بالعمليات الحقيقية
- دليل `ECONOMY.md`

---

## v3 — بناء الإمبيدات والأوامر

- محرّر إمبيد تفاعلي كامل داخل ديسكورد
- أزرار وقوائم بأربعة أنواع إجراءات
- تتبّع الرسائل المنشورة وتحديثها بزر واحد
- أوامر مخصصة ببريفكس خاص لكل أمر
- دليل `BUILDER.md`

---

## v2 — إصلاحات التشغيل

- `index.js` في الجذر ليعمل مع بتردكتل بلا تعديل إعدادات
- محرّك قاعدة بيانات بديل يعمل بلا `better-sqlite3` (SQLite المدمج في Node 22)
- الزب ينفك مباشرة في مكانه بلا مجلد ملتف
- `.env` جاهز داخل الملف
- نشر أوامر السلاش تلقائيًا عند الإقلاع

---

## v1 — الإصدار الأول

- الأساس: قاعدة بيانات، محرك صلاحيات، نظام أوامر موحّد، معالجة أخطاء، ترجمة
- الإدارة الكاملة + نظام القضايا المرقّمة
- السجلات، السلم الإداري، إحصائيات النشاط
- التذاكر باستلام ذرّي
- الرتب الذاتية والتلقائية، الحماية، السحوبات، الاستطلاعات، الإذاعة

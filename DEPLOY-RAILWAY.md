# النشر على Railway مع GitHub

دليل كامل من الصفر. بعده البوت يشتغل 24/7 ويتحدّث تلقائياً مع كل تعديل ترفعه.

---

## أولاً: قرار قاعدة البيانات — اقرأ هذا

Railway يعرض عليك PostgreSQL و MySQL و MongoDB. **لا تحتاج أياً منها**، وهذا ليس اختصاراً بل القرار الهندسي الأصح لبوت ديسكورد:

| | SQLite + Volume | Postgres على Railway |
|---|---|---|
| سرعة الاستعلام | ميكروثانية (محلي) | مللي ثانية (عبر الشبكة) |
| المعاملات الذرّية | متزامنة ومضمونة | تحتاج إعادة كتابة كل الأنظمة |
| نقاط الفشل | لا شيء | انقطاع الشبكة يوقف البوت |
| التكلفة | مجاني ضمن الخطة | خدمة إضافية |
| الإعداد | نقرتان | متغيرات + هجرات + إعادة كتابة |

**السبب التقني:** كل ضمانات البوت ضد التكرار — استلام التذكرة، سداد المخالفة، حجز المقعد، قبول الطلب — مبنية على معاملات SQLite **المتزامنة**. تحويلها إلى Postgres يعني تحويل كل دالة إلى `async` وإعادة اختبار كل حالة سباق من الصفر. مكسب معدوم مقابل مخاطرة عالية.

**البوت عملية واحدة، لا عشر عمليات تتنافس على البيانات.** SQLite هنا ليس حلاً مؤقتاً، هو الحل الصحيح.

> إن احتجت Postgres مستقبلاً (مثلاً لوحة تحكم ويب تقرأ نفس البيانات)، طبقة المستودعات معزولة بالكامل — راجع `ARCHITECTURE.md` قسم 3.

---

## الخطوة 1: ارفع المشروع على GitHub

على جهازك، داخل مجلد المشروع:

```bash
git init
git add .
git commit -m "الإصدار الأول"
git branch -M main
git remote add origin https://github.com/اسمك/اسم-المستودع.git
git push -u origin main
```

> `.gitignore` جاهز ويستثني `.env` و `data/` تلقائياً. **تأكد أن `.env` ما انرفع** — افتح المستودع على GitHub وتحقق.

لو انرفع بالغلط: سوّي **Reset Token** فوراً من بورتال ديسكورد.

---

## الخطوة 2: أنشئ المشروع على Railway

1. [railway.app](https://railway.app) ← سجّل دخول بـ GitHub
2. **New Project** ← **Deploy from GitHub repo**
3. اختر المستودع
4. Railway يقرأ `railway.json` و `nixpacks.toml` تلقائياً ويبني بـ Node 22

---

## الخطوة 3: أضف Volume — هذه أهم خطوة

**بدونها تفقد كل البيانات مع كل نشر جديد.**

في صفحة الخدمة:

1. تبويب **Variables** ← اضغط **+ New Variable** ← اختر **Add Volume**
2. أو: **Settings** ← **Volumes** ← **Add Volume**
3. **Mount Path:** اكتب بالضبط:
   ```
   /app/data
   ```
4. احفظ

هذا يعطيك قرصاً دائماً ينجو من كل عمليات النشر.

---

## الخطوة 4: متغيرات البيئة

تبويب **Variables** ← أضف:

| المتغير | القيمة |
|---|---|
| `BOT_TOKEN` | توكن البوت |
| `CLIENT_ID` | آيدي التطبيق |
| `DEVELOPER_IDS` | آيديك (وشركاءك بفاصلة) |
| `DATABASE_PATH` | `/app/data/bot.db` |
| `NODE_ENV` | `production` |

**اختيارية:**

| المتغير | الافتراضي | الشرح |
|---|---|---|
| `DEV_GUILD_ID` | — | آيدي سيرفرك: أوامر السلاش تظهر فوراً بدل ساعة |
| `BACKUP_INTERVAL_HOURS` | `6` | كل كم ساعة نسخة احتياطية |
| `BACKUP_KEEP` | `10` | كم نسخة تُحفظ |
| `LOG_LEVEL` | `info` | `debug` للتفاصيل الكاملة |

> `PORT` يضبطه Railway تلقائياً — **لا تضفه يدوياً**.

---

## الخطوة 5: شغّل وتأكد

Railway ينشر تلقائياً. راقب **Deployments** ← **View Logs**، ولازم تشوف:

```
محرّك قاعدة البيانات: SQLite المدمج في Node
قاعدة البيانات جاهزة: /app/data/bot.db
تم تحميل 54 أمر من 21 نظام.
خادم فحص الصحة يعمل على المنفذ ...
تم نشر 54 أمر سلاش
البوت جاهز.
```

### تأكد من الحفظ الدائم

هذي أهم خطوة تحقق:

1. في ديسكورد: `/embed create name:اختبار` واحفظ فيه أي شي
2. في Railway: **Deployments** ← **Redeploy**
3. بعد ما يشتغل: `/embed list`

**لو ظهر `اختبار` فالبيانات محفوظة بشكل دائم.** لو اختفى، الـ Volume غير مربوط — راجع الخطوة 3.

---

## الخطوة 6: افحص كل شي

في ديسكورد اكتب:

```
/checkup
```

يعطيك تقريراً كاملاً:
- 🔴 **أخطاء** — أنظمة مفعّلة لكن ينقصها قناة أو رتبة، أو مراجع محذوفة
- 🟡 **تنبيهات** — صلاحيات ناقصة، قنوات سجلات غير مربوطة، رتب أعلى من البوت
- 🟢 **سليم**

عالج الأحمر أولاً. هذا الأمر يكشف المشاكل **قبل** ما تظهر أمام الأعضاء.

---

## التحديث التلقائي

بعد الربط، أي تعديل ترفعه ينشر تلقائياً:

```bash
git add .
git commit -m "وصف التعديل"
git push
```

Railway يبني وينشر خلال دقيقة. و GitHub Actions يفحص الكود قبلها (`.github/workflows/ci.yml`) ويرفض أي ملف فيه خطأ صياغة أو سر مسرّب.

---

## النسخ الاحتياطي

يعمل تلقائياً كل 6 ساعات في `/app/data/backups/` مع الاحتفاظ بآخر 10 نسخ.

**للاستعادة:** أوقف الخدمة، انسخ ملف النسخة فوق `bot.db`، شغّل من جديد.

**لتنزيل نسخة:** Railway ← تبويب **Data** أو عبر `railway run` من جهازك:
```bash
railway run cat /app/data/backups/bot-2026-08-29T00-00-00.db > backup.db
```

---

## مراقبة الصحة

البوت يفتح مسارات فحص تلقائياً:

```
GET /health
```
```json
{
  "status": "ok",
  "uptimeSeconds": 3600,
  "guilds": 3,
  "commands": 54,
  "database": "connected",
  "version": "9.0.0"
}
```

Railway يستخدمه لإعادة التشغيل التلقائي عند التعطل. وتقدر تربطه بـ [UptimeRobot](https://uptimerobot.com) مجاناً ليصلك تنبيه لو وقف.

---

## حل المشاكل

| المشكلة | الحل |
|---|---|
| البيانات تختفي بعد النشر | الـ Volume غير مربوط على `/app/data`، أو `DATABASE_PATH` غلط |
| `لا يوجد محرّك قاعدة بيانات` | إصدار Node أقل من 22 — `nixpacks.toml` يضبطه، تأكد أنه مرفوع |
| `متغيرات بيئة ناقصة` | راجع Variables — الأسماء حساسة لحالة الأحرف |
| Healthcheck يفشل | طبيعي أول دقيقة أثناء تسجيل الدخول. لو استمر، راجع اللوق |
| أوامر السلاش ما تظهر | تُنشر تلقائياً عند الإقلاع. حط `DEV_GUILD_ID` لتظهر فوراً |
| البوت يعيد التشغيل باستمرار | راجع اللوق — غالباً توكن خاطئ أو نية مميزة غير مفعّلة |

**تشخيص سريع من ديسكورد:** `/dev diagnostics` للمطور، و `/checkup` للإدارة.

---

## قائمة تحقق نهائية

- [ ] المشروع على GitHub و `.env` **غير** مرفوع
- [ ] Volume مربوط على `/app/data`
- [ ] `DATABASE_PATH` = `/app/data/bot.db`
- [ ] `BOT_TOKEN` و `CLIENT_ID` و `DEVELOPER_IDS` مضبوطة
- [ ] **Server Members** و **Message Content** مفعّلتان في بورتال ديسكورد
- [ ] رتبة البوت فوق كل الرتب اللي بيديرها
- [ ] اختبار إعادة النشر أثبت بقاء البيانات
- [ ] `/checkup` ما فيه أخطاء حمراء

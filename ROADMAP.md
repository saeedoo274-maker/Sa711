# خارطة الطريق — ما هو منجز وما هو مؤجل

هذا المستند صريح تمامًا. المواصفات في القسم 44 تمنع "Fake Features"، لذا لا يوجد في المشروع أي زر لا يعمل أو إعداد لا يُحفظ. وبنفس المنطق، ما لم يُبنَ مذكور هنا بوضوح بدل ادّعاء اكتماله.

---

## ✅ منجز ومُختبَر (المراحل 1–9)

### المرحلة 1 — البنية والأساس وقاعدة البيانات
- هيكلية Modular بفصل حقيقي بين الطبقات
- SQLite بمعاملات ACID + نظام هجرات تلقائي
- 9 مستودعات بيانات معزولة عن باقي المشروع
- إعدادات مركزية: `config/bot.json` + `guild-defaults.json` + `.env`
- Multi-Guild بعزل كامل ومُختبَر بين السيرفرات
- نظام ترجمة كامل في `locales/ar.json`

### المرحلة 2 — نظام الأوامر ومحرك الصلاحيات
- 39 أمرًا تعمل بـ Slash و Prefix و بدون Prefix من كود واحد (`CommandContext`)
- محرك صلاحيات مركزي بستة مستويات + فحص هرمية شامل
- Aliases، Cooldowns، تعطيل أوامر لكل سيرفر، وضع صيانة
- `/help` يُولَّد تلقائيًا من البيانات الوصفية للأوامر

### المرحلة 3 — الإدارة
- حظر، فك حظر، طرد، إسكات، فك إسكات، تحذير، حذف تحذير
- حذف رسائل، قفل/فتح قناة، وضع بطيء
- إدارة الرتب والأسماء
- إدارة صوتية كاملة: كتم، إصمام، نقل، فصل
- معلومات العضو، السجل الإداري، عرض القضايا
- **نظام Moderation Cases** برقم متسلسل مستقل لكل سيرفر

### المرحلة 4 — السجلات
- قناة منفصلة لكل نوع سجل (9 أنواع)
- تعمل عبر EventBus بفصل تام عن الأنظمة المُنتِجة للأحداث
- **نظام سجل أخطاء مستقل** برقم خطأ لكل حادثة، بدون تسريب Stack للمستخدم

### المرحلة 5 — السلم الإداري والإحصائيات
- سلم إداري مرتّب بأسماء ومستويات قابلة للضبط
- ترقية وتنزيل بضغطة واحدة بعد 5 فحوصات متتالية
- تتبّع نشاط الطاقم: الرسائل، الوقت الصوتي، التذاكر (مُجمَّع يوميًا)
- لوحة صدارة وملف نشاط لكل إداري

### المرحلة 6 — التذاكر
- لوحات تذاكر مخصّصة (عنوان، وصف، لون، بانر، صورة مصغّرة، زر، إيموجي، كاتيغوري)
- **استلام ذرّي** بلا Race Conditions — مُختبَر
- إغلاق، إعادة فتح، حذف (لا يظهر إلا بعد الإغلاق)
- إضافة/إزالة عضو، السماح بالملفات، إعادة تسمية، نسخة محادثة
- الخصوصية عبر **Permission Overwrites حقيقية** على القناة، لا بإخفاء الواجهة

### المرحلة 7 — الرتب
- رتب ذاتية بقائمة اختيار أو أزرار، بحد أقصى قابل للضبط
- رتب تلقائية للأعضاء والبوتات الجدد
- حماية: رفض الرتب المُدارة، والرتب الأعلى من البوت، ورتب Administrator

### المرحلة 8 — السحوبات والاستطلاعات والإذاعة
- سحوبات بشروط دخول (رتبة مطلوبة، عمر الحساب) وفرص إضافية
- **إنهاء ذرّي** يمنع سحب فائزين مرتين — مُختبَر
- إعادة سحب، إلغاء، إنهاء يدوي، قائمة السحوبات النشطة
- استطلاعات حتى 5 خيارات، أحادي أو متعدد، نتائج مخفية أو ظاهرة
- إذاعة في قناة أو لخاص رتبة، مع تأكيد وتنظيم سرعة وعدّ النجاح والفشل

### المرحلة 9 — الحماية
- 7 قواعد: إنشاء/حذف القنوات، إنشاء/حذف الرتب، منح رتبة عالية، تصعيد صلاحيات، إساءة البوتات
- لكل قاعدة: تفعيل، حد، نافذة زمنية، إجراء وقائي، قائمة بيضاء
- الرصد عبر Audit Log مع نسب الإجراء لمنفّذه الحقيقي

### المرحلة 14 — بناء الإمبيدات والأوامر المخصصة
- **محرّر إمبيد تفاعلي كامل** داخل ديسكورد: نصوص، صور، ألوان، فوتر، كاتب، حقول، وقت
- **أزرار وقوائم منسدلة** بأربعة أنواع إجراءات: فتح إمبيد آخر، رابط خارجي، إعطاء/سحب رتبة، رد نصي
- **تتبّع الرسائل المنشورة**: تعديل الإمبيد ثم زر واحد يحدّث كل الرسائل في كل القنوات
- **أوامر مخصصة** ببريفكس خاص لكل أمر، ومستوى صلاحية، ورد مخفي، وحذف رسالة الأمر
- متغيرات ديناميكية: `{user}` `{username}` `{server}` `{membercount}`
- حمايات: قص تلقائي على كل حدود ديسكورد، روابط https فقط، منع توزيع رتب Administrator، منشن جماعي معطّل افتراضيًا
- الدليل الكامل في **BUILDER.md**

### المرحلة 15 — الاقتصاد: البنك والمخالفات والطيران
- **البنك**: رصيد نقدي ومصرفي، تحويل برسوم وسقف، إيداع، سحب، سجل حركات دائم لا يُحذف، لوحة الأغنى
- **المخالفات**: ترقيم متسلسل لكل سيرفر، سداد يخصم فعليًا من الرصيد، إلغاء، إشعار خاص، رتبة إيقاف خدمات تلقائية عند تجاوز الحد وتُسحب تلقائيًا عند السداد
- **الطيران**: رحلات بسعر وسعة وكابتن ووقت إقلاع، حجز يخصم الثمن، إلغاء يسترجع، قائمة ركاب، إغلاق الرحلة
- **الربط باللوحات**: 13 إجراء (`sys:bank:balance` وأخواته) تُربط بأزرار وقوائم الإمبيدات المبنية يدويًا، فتصير اللوحة منفّذًا حقيقيًا لا واجهة شكلية
- ضمانات مالية مُختبَرة: لا رصيد سالب، لا خلق مال، لا سداد مزدوج، لا بيع مقعد مرتين، لا مقعد محجوز بلا دفع
- الدليل الكامل في **ECONOMY.md**

### المرحلة 16 — أنواع التذاكر والقوائم المتداخلة
- **أنواع تذاكر** لكل خدمة: كاتيغوري ورتبة دعم وقالب اسم قناة وإمبيد ترحيب خاص بكل نوع
- **نموذج أسئلة** حتى 5 أسئلة يعبّيها العضو قبل فتح القناة، والإجابات تُنشر للطاقم تلقائيًا
- **حد تذاكر مستقل لكل نوع**، فيقدر العضو يفتح تذكرة لكل خدمة بالتوازي
- **قوائم متداخلة بلا حد**: خيار يفتح إمبيد فيه قائمته الخاصة، وخيار يرجّع للوحة الأم
- إجراء `ticket:اسم_النوع` يربط أي زر أو خيار قائمة مبني يدويًا بفتح تذكرة
- الدليل الكامل في **PANELS.md**

### المرحلة 17 — التقديمات واختبار التفعيل
- **نظام تقديمات** كامل: أنواع بأسئلة وقناة مراجعة ورتبة قبول ومهلة إعادة تقديم
- طلبات مرقّمة بأزرار قبول/رفض/رفض بسبب، والقرار **ذرّي** فلا يُبت في الطلب مرتين
- إعطاء وسحب الرتب تلقائيًا عند القبول، وإشعار العضو في الخاص برسالة قابلة للتخصيص
- **تدفق أسئلة في الخاص** سؤالاً بعد سؤال، يتجاوز حد المودال (5) ويقبل الصور والأسئلة الرقمية
- **اختبار تفعيل**: بنك أسئلة، أزرار متسلسلة مربوطة بصاحبها، درجة نجاح، ورتبة تلقائية
- إجراءات `apply:اسم_النوع` و `quiz` للوحات المبنية يدويًا
- الدليل الكامل في **APPLICATIONS.md**

### المرحلة 18 — محرّر التقديمات التفاعلي
- محرّر بالأزرار لكل خصائص نوع التقديم، مع معاينة حية للطلب قبل النشر
- تعديل الأسئلة فرديًا: إضافة، تعديل، حذف، إعادة ترتيب
- زخرفة الإمبيد: عنوان بمتغيرات، لون، صور، فوتر، إيموجي الحقول
- نظام منشن بالرتب و`@everyone`/`@here`
- تحقق مسبق من صلاحيات القناة وتسلسل الرتب قبل الحفظ، فلا يُكتشف الخلل وقت القبول

### المرحلة 19 — دورة حياة الطاقم (دمج c2 — المرحلة 1)
- **الإجازات**: حفظ الرتب المسحوبة واستعادتها بدقة، إنهاء تلقائي عند انتهاء المدة، رتب معفاة
- **الاستقالات**: سحب رتب الطاقم مع حفظها للسجل
- **البلاغات على الإداريين**: أدلة وشهود، وتحذيرات متصاعدة تلقائيًا مع إشعار الإدارة العليا عند الحد الأقصى
- كل القرارات ذرّية، والترقيم مستقل لكل نوع ولكل سيرفر
- سحب الرتب يحترم: الرتب المعفاة، الرتب المُدارة، وتسلسل رتبة البوت

### المرحلة 20 — الردود التلقائية وجاهزية النشر (دمج c2 — المرحلة 2)
- ردود تلقائية بأربعة أنواع مطابقة وتوحيد كامل للنص العربي
- شروط القنوات والرتب والتبريد ونسبة الرد، وأمر تجربة قبل النشر
- لوحة النجوم بعدّاد يتحدّث تلقائيًا
- `/checkup`: فحص شامل يكشف كل خلل في الإعداد قبل ظهوره للأعضاء
- خادم صحة، ونسخ احتياطي دوري، وملفات نشر Railway و GitHub Actions

### المرحلة 21 — مراقبة السيرفرات وحماية البوت
- لوحة `/guilds` للمطورين: قائمة، تفاصيل، إحصائيات، مغادرة، وحظر
- قائمة سوداء تمنع العمل وتُغادر تلقائيًا حتى عند إعادة الدعوة
- حارس إساءة بتصعيد تدريجي: تبريد ثم حظر تلقائي، لحماية موارد الاستضافة
- `/lockdown` إغلاق طارئ قابل للعكس بدقة مع حفظ حالة كل قناة

### المرحلة 22 — تحسينات التذاكر (دمج c2 — المرحلة 3)
- ترقيم متسلسل لكل سيرفر يظهر في اسم القناة والأرشيف
- تقييم الخدمة بالنجوم بعد الإغلاق، مع متوسطات لكل إداري ولوحة صدارة
- إغلاق تلقائي على مرحلتين عند الخمول: تنبيه ثم قفل أو حذف مع أرشفة
- حد التذاكر المستلمة لكل موظف
- ترانسكربت HTML بتصميم عربي وتهريب كامل ضد XSS

### المرحلة 23 — التقارير الدورية والدلائل (دمج c2 — المراحل 3 و4)
- تقارير يومية أو أسبوعية بستة أقسام قابلة للاختيار، مع منع التكرار في نفس اليوم
- كشف الطاقم غير النشط حسب حد أدنى قابل للضبط
- توثيق العقوبات بالأدلة والروابط، مربوطًا بالقضايا الإدارية

### المرحلة 24 — إكمال الدمج (c2 — المراحل 4 و5، الأخيرتان)
- رد التفاعلات: رتبة/نص/إمبيد عند الضغط على إيموجي محدد
- المنشورات المميّزة: أمر وقائمة سياق (right-click)، ذرّي ضد التكرار
- سجل الإصدارات يُبث تلقائيًا لكل سيرفر مشترك مع تتبّع الاستلام
- Webhook GitHub حقيقي بتحقق HMAC، على نفس خادم الصحة الموجود

### المرحلة 25 — لوحة التحكم المركزية /devpanel
- توسعة `/لوحة` (بديل `/devpanel`) لتغطي كل الأنظمة الإدارية بدل أوامر منفصلة
- 12 شاشة جديدة: التذاكر، دورة حياة الطاقم، التقارير، التفاعل والمحتوى، الدلائل، الحماية، مراقبة السيرفرات، الفحص الشامل
- فحص صلاحية عند كل تفاعل داخل اللوحة، لا فقط عند الفتح
- صفر أوامر محذوفة — طبقة تحكم فوق الخدمات الموجودة فقط

### المرحلة 12 — الاختبار والتدقيق الأمني
- **587 اختبارًا ناجحًا، صفر فاشل**
- تدقيق أمني: لا أسرار في الكود، كل استعلامات SQL معاملة، الحقول الديناميكية على قائمة بيضاء، فحص صلاحيات من الخادم في كل معالج

---

## ⏳ مؤجل — لم يُبنَ (المراحل 10، 11، 13)

### 🔴 لوحة التحكم الويب و OAuth2 و API (المراحل 10–11)
**الحالة: لم يُكتب فيها أي سطر كود.**

هذا هو أكبر جزء غير منجز، وأذكره أولًا لأنه الأوضح غيابًا. المطلوب في المواصفات (الأقسام 35–40) هو منظومة كاملة:
- خادم ويب + جلسات آمنة + Discord OAuth2
- عرض السيرفرات التي يديرها المستخدم
- صفحة إعدادات لكل نظام من أنظمة البوت
- تحقق من الخادم في كل عملية + CSRF + Rate Limiting
- API محمية تربط الموقع بالبوت

بناؤها بشكل حقيقي وآمن يحتاج جلسة عمل مستقلة كاملة. البنية الحالية جاهزة لاستقبالها: قاعدة البيانات ومحرك الصلاحيات و`GuildConfigService` كلها قابلة للاستخدام من خادم ويب مباشرة بلا تعديل.

**البديل المتاح الآن:** كل إعدادات البوت قابلة للضبط من `/panel` داخل ديسكورد.

### ✅ Embed Builder — بُني بالكامل
انظر قسم المرحلة 14 بالأعلى.

### 🟡 الأنظمة الإضافية المقترحة (القسم 49)
مقترحة في المواصفات ولم تُبنَ:
- نظام المستويات (Level System)
- AFK
- الاقتراحات (Suggestions)
- Starboard
- التذكيرات (Reminders)
- أتمتة التفاعلات (Reaction Automation)

> ملاحظة: **Moderation Cases** كان ضمن هذه القائمة وقد **بُني بالكامل**.

### 🟡 توسعات لأنظمة قائمة
- **إحصائيات السيرفر:** الموجود حاليًا لوحة أساسية في `/panel`. الإحصائيات الموسّعة (رسوم بيانية، نمو الأعضاء) غير مبنية.
- **سجل تاريخ العضو:** `/history` يعرض السجل الإداري. تتبّع تغيّر الأسماء والرتب عبر الزمن غير مبني.
- **الإذاعة للمتصلين فقط:** الموجود إذاعة بالرتبة. الفلترة بحالة الاتصال تحتاج `GuildPresences` intent وهو غير مفعّل.

### 🟡 المرحلة 13 — النشر الإنتاجي
المشروع **جاهز للنشر** (متغيرات بيئة، لا أسرار في الكود، معالجة أخطاء شاملة، إغلاق نظيف عند SIGTERM)، لكن لم تُجهَّز ملفات نشر خاصة (Dockerfile، إعدادات منصة معيّنة).

---

## ⚠️ قيود يجب معرفتها

| القيد | التفصيل |
|---|---|
| **لم يُختبر على ديسكورد حيًا** | الشبكة كانت معطّلة أثناء البناء، فتعذّر `npm install` وتعذّر الاتصال. اختُبر المنطق كاملًا (122 اختبارًا) عبر تشغيل حقيقي فوق `node:sqlite` وشيم يتحقق من صحة بناء أوامر السلاش. **أول تشغيل عندك هو الاختبار الحي الأول.** |
| **تسلسل رتب ديسكورد** | البوت لا يستطيع التصرف في عضو رتبته أعلى من رتبة البوت — حتى لو كان الطالب مطورًا. قيد من ديسكورد نفسه لا التفاف عليه. |
| **نظام الحماية يحتاج Audit Log** | بدون صلاحية View Audit Log لا يستطيع النظام نسب الإجراءات لمنفّذيها، فلن يعمل. |
| **حذف الرسائل** | ديسكورد يمنع الحذف الجماعي للرسائل الأقدم من 14 يومًا. البوت يفلترها تلقائيًا ويخبرك بالعدد الفعلي المحذوف. |
| **الإسكات** | الحد الأقصى 28 يومًا (قيد ديسكورد). |
| **الإذاعة للخاص** | كثير من الأعضاء يغلقون الخاص، والفشل معهم طبيعي ويُحتسب في تقرير النتيجة. |

---

## الخطوة التالية المقترحة

الترتيب حسب الأثر:

1. **شغّل البوت واختبره حيًا** — هذا يحسم أي مفاجأة متبقية من قيود الشبكة.
2. **لوحة التحكم الويب + OAuth2** — أكبر جزء ناقص، والبنية جاهزة لاستقباله.
3. **Embed Builder تفاعلي** — أعلى فائدة مقابل جهد متوسط.
4. **الأنظمة الإضافية** حسب حاجتك الفعلية، لا كلها دفعة واحدة.

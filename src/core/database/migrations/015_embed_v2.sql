-- ============================================================
--  الشكل الحديث للإمبيدات (Components v2)
--  عند تفعيله تُعرض الأزرار داخل الحاوية الملوّنة نفسها
--  بدل ظهورها منفصلة تحت الإمبيد التقليدي.
-- ============================================================

ALTER TABLE embeds ADD COLUMN use_v2 INTEGER NOT NULL DEFAULT 0;
